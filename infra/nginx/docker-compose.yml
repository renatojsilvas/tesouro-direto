services:
  nginx:
    image: nginx:alpine
    container_name: tesouro-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - tesouro-net
    restart: unless-stopped

networks:
  tesouro-net:
    external: true
