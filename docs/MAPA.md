# Mapa do Sistema — Tesouro Direto API

> Mapeamento por área (rotas/entradas, modelo de dados, integrações externas, jobs/observabilidade, testes).
> Gerado por análise **somente-leitura** do código. Não altera comportamento.
> As fragilidades marcadas com ✅ foram confirmadas contra o código pelo revisor; ⚠️ = nuance; ❌ = refutada. **✔️ RESOLVIDO** = já corrigida (tarefa do [PLANO](./PLANO.md) + data). Ver seção [Verificação](#verificação-das-fragilidades).

## Arquitetura em uma frase

Solução .NET 8 em **Clean Architecture / Ports & Adapters**, mono-domínio com contextos (Titulos, PrecosTaxas, Tributos, Simulador, Feriados, DiasUteis). CQRS via **MediatR**: escritas com **EF Core**, leituras com **Dapper** (devolvendo DTOs). Duas entradas: **API Minimal** (`TesouroDireto.API`) e **Blazor Server** (`TesouroDireto.Web`) que consome a API por HTTP. Ingestão de dados por **importação agendada (Quartz)** de fontes públicas (Tesouro Transparente, ANBIMA, BCB Focus). Observabilidade com Serilog + Prometheus coletados por um **Grafana Alloy** único (scrape/tail local) e reenviados ao **Grafana Cloud** (free tier) via `remote_write`/`loki.write` — substituiu a stack local `grafana`+`loki`+`prometheus`+`promtail`+`node-exporter` na tarefa 77 (2026-08-15).

```
                 ┌─────────────────┐         ┌──────────────────┐
  navegador ───► │ Web (Blazor SSR) │──HTTP──►│  API (Minimal)   │
                 └─────────────────┘ X-Api-Key└────────┬─────────┘
                                                        │ ISender (MediatR)
                                            ┌───────────▼───────────┐
                                            │      Application       │  commands/queries + ports
                                            └───┬───────────────┬────┘
                          EF Core (write) ◄─────┘               └─────► Dapper (read, +cache decorators)
                                                      ┌───────────────────────┐
   Tesouro Transparente (CSV)           ──Quartz────►│    Infrastructure     │──► PostgreSQL
   ANBIMA (XLS, Quartz anual + manual)  ──HTTP───────►│  adapters / persist.  │
   BCB Focus (OData, cache 6h + lkg 7d) ──HTTP───────►└───────────────────────┘
```

Projetos: `API`, `Web`, `Application`, `Domain`, `Infrastructure` + 6 projetos de teste.

---

## 1. Rotas / Entradas

### O que existe

**API — Minimal API** (`src/TesouroDireto.API/Program.cs`). Composição por camada (tarefa 55, 2026-08-06): `AddSerilog` → `AddApplication` (MediatR + `LoggingBehavior`) → `AddInfrastructure(IConfiguration)` → `AddApiServices` (health `AddDbContextCheck`, swagger, problem+json, JSON enum) — cada camada dona do seu `DependencyInjection.cs`, `Program.cs` só orquestra. Pipeline HTTP: **`ApiKeyGuard.Validate` (aborta o boot em ambiente != Development/Testing se `ApiKey:Key` for vazia ou o default — antes da migração)** → migração automática (exceto env `Testing`) → `UseSerilogDefaults` (CorrelationId + request logging) → `UseHttpMetrics` → `ApiKeyMiddleware` → rotas. Cada endpoint é **fino**: injeta `ISender`, faz `Send(command/query)` e traduz `Result`→`Results.*`. Sem lógica de negócio nos endpoints.

| Método | Rota | Comando/Query | Status |
|--------|------|---------------|--------|
| GET | `/health`, `/health/ready` | `AddDbContextCheck` (readiness, isento de ApiKey) | 200/503 |
| GET | `/health/live` | liveness (`Predicate=_=>false`, não toca DB, isento) | 200 |
| GET | `/metrics` | Prometheus (isento) | 200 |
| POST | `/importacao` | `ImportCsvCommand` (sem body) | 200/400 |
| POST | `/importacao/feriados` | `ImportFeriadosCommand` (sem body) | 200/400 |
| GET | `/titulos?indexador&vencido` | `GetTitulosQuery` (itens com `_links`) | 200/400 |
| GET | `/titulos/{codigo}` | `GetTituloByCodigoQuery` (recurso único, `_links` self/precos/preco-atual/simular) | 200/400/404 | ← tarefa 39 |
| GET | `/titulos/{codigo}/precos?dataInicio&dataFim&page&pageSize` | `GetPrecosByCodigoQuery` | 200/400/404 | ← tarefa 36; paginação opcional + `Link`/`X-Total-Count` na 39 |
| GET | `/titulos/{codigo}/preco-atual` | `GetPrecoAtualByCodigoQuery` | 200/400/404 | ← tarefa 36 |
| GET | `/titulos/{id}/precos?dataInicio&dataFim` | `GetPrecosQuery` | 200/404 | **DEPRECATED** (tarefa 36; removida na 38) |
| GET | `/titulos/{id}/preco-atual` | `GetPrecoAtualQuery` | 200/404 | **DEPRECATED** (tarefa 36; removida na 38) |
| GET | `/titulos/preco-atual?nome` | `GetPrecoAtualByNomeQuery` | 200/404 |
| GET | `/titulos/precos?nome&dataInicio&dataFim` | `GetPrecosByNomeQuery` | 200/404 |
| GET | `/configuracoes/tributos` | `GetTributosQuery` | 200/400 |
| GET | `/configuracoes/tributos/{id}` | `GetTributoByIdQuery` (recurso único; alvo do `Location` do 201) | 200/404 | ← tarefa 39 |
| POST | `/configuracoes/tributos` | **`CreateTributoCommand` (corpo ligado direto)** — `Location` via `CreatedAtRoute` | 201/400 |
| PUT | `/configuracoes/tributos/{id}` | `UpdateTributoCommand` (via `UpdateTributoRequest`) | 204/404/400 |
| POST | `/simulador` | `SimularCommand` (via `SimularRequest`) | 200/400 |
| POST | `/simulador/cenarios` | `SimularCenariosCommand` | 200/400 |

**Maturidade REST (tarefa 39):** todas as rotas GET de leitura passam por `ReadEndpointExtensions.MapReadGet` (GET+HEAD+OPTIONS; método não suportado → 405 com `Allow: GET, HEAD, OPTIONS`) e pelo `ConditionalGetFilter` (endpoint filter): ETag = hash de token de versão global (`IContentVersionProvider`: `max(data_base)`+counts) + método+path+query; `If-None-Match` casando → 304; `ETag`/`Cache-Control: public, max-age=300` **só em respostas 2xx** (erro não é cacheável). Simulador → `Cache-Control: no-store`. Paginação in-memory em `/titulos/{codigo}/precos` (`page`/`pageSize`, defaults preservam o comportamento anterior) com `Link` (RFC 8288) + `X-Total-Count`; corpo segue array cru. `_links` HAL-like só nos itens de título (sibling; raiz segue array). nginx: gzip (`gzip_vary on`) + `limit_req_status 429`+`Retry-After`. **Regra para todo endpoint futuro.**

**Prefixo público nos links (tarefa 75, 2026-08-09):** o nginx atende `location /api/` com `proxy_pass …:5000/` — a **barra final corta o `/api`** —, então até aqui `_links` e o header `Link` saíam como `/v1/...` e **não funcionavam para quem chama de fora** (resolvidos contra o host, apontavam para caminho inexistente). Corrigido **por requisição**, não globalmente: o nginx envia `X-Forwarded-Prefix: /api` nos dois `location /api/`, e `Http/ForwardedPrefixMiddleware` (após `UseForwardedHeaders`, **antes do roteamento**) seta `Request.PathBase` — o `Path` continua `/v1/...` (roteamento e `ApiKey:ExcludedPaths` intactos, ambos casam em `Path`) e o `LinkGenerator` concatena. **Não usa `UsePathBase`**, que faria o oposto (remover o prefixo já removido pelo nginx). Cobre `_links`, o header `Link` **e** o `Location` de `CreatedAtRoute`. Por requisição **porque o Web/BFF chama `http://app:8080` direto e segue os mesmos `_links`** — prefixo global quebraria `Historico`/`Simulador`. O header é forjável por quem alcança a API direto, então só vale se casar **exatamente** (ordinal, valor único) com `ForwardedPrefix:Allowed` (`["/api"]`); allowlist ausente ⇒ nenhum prefixo.

**Middleware:** `ApiKeyMiddleware` (header `X-Api-Key`, comparação SHA256 em tempo constante, isenta `/health` e `/metrics`, falha→401 com corpo `application/problem+json`+`correlationId` desde a tarefa 6); `CorrelationIdMiddleware` (header `X-Correlation-Id`, valida regex, injeta no LogContext **e em `HttpContext.Items`** para leitura no corpo do ProblemDetails). **Autorização admin (tarefa 62, BFF-trust):** as rotas `/admin/usuarios/{pendentes,aprovar,desativar}` levam `AdminOnlyFilter` (`API/Http/`, `IEndpointFilter`) que lê o header `X-Acting-User-Sub` (google_sub do usuário asserido pelo Web), carrega o `Usuario` e exige `Papel=Admin`+`aprovado`+`ativo`, senão **403 problem+json**; guarda o admin em `HttpContext.Items` (fonte de `aprovado_por`). O `sync` fica **fora** do filtro (1º login). A sessão Google que popula esse header nasce na 64.

**Web — Blazor Server** (`src/TesouroDireto.Web`): Razor Components (InteractiveServer) + **typed client `TesouroApiClient`** (`Services/TesouroApiClient.cs`, tarefa 16, 2026-07-25) sobre `AddHttpClient<TesouroApiClient>` (BaseUrl + header `X-Api-Key` de config, `.AddHttpMessageHandler<CorrelationIdHandler>()` — O2). Encapsula `GetAsync<T>`, `GetPagedAsync<T>`, `PostAsync<T>`/`PutAsync<T>` (retornam `ApiResult<T>` com `IsSuccess`/`Data`/`ApiError Error`/`StatusCode`/`RawBody`) e o `ApiError(Code, Description)` compartilhado. **Consumo REST maduro (tarefa 40, 2026-07-27):** `GetAsync<T>` faz **GET condicional** (envia `If-None-Match`, reusa o corpo no `304`) via `IConditionalGetStore`/`BoundedConditionalGetStore` — store **singleton** (LRU cap 128) compartilhado entre circuitos (o client é transient; sem singleton, cada reload nasceria sem cache e o 304 nunca aconteceria), guardando ETag+bytes+snapshot de `X-Total-Count`/`Link` (cobre todos os reads, inclusive páginas de precos). `GetPagedAsync<T>` devolve `PagedResponse<T>(Items, TotalCount, PageLinks)` lendo os headers. Navegação por `_links` (precos via `_links.precos`, Simulador via `_links.simular`); `Historico` pagina de verdade pelos hrefs do header `Link` (sem `TakeLast(50)`). Páginas em `Components/Pages/`: `Titulos`, `Historico` (gráfico via JS interop, paginação real), `Tributos`, `Simulador`, `Cenarios`, `Home`, `About`, `Error` — **todas as 5 páginas de dados injetam `TesouroApiClient`** (nenhuma monta `IHttpClientFactory`/`HttpClient` nem desserializa erro manualmente). O `HttpClient` nomeado `"TesouroDiretoApi"` foi **removido** no 16f (sem consumidores).

**Web — páginas Blazor** (`src/TesouroDireto.Web/Components/Pages/`, 10 arquivos):

| Página | Rota | Auth | Render mode | Propósito |
|---|---|---|---|---|
| `Home.razor` | `/` | — | estático (sem `@rendermode`) | Landing / hero de apresentação |
| `Titulos.razor` | `/titulos` | — | `InteractiveServer` | Lista e filtra os títulos públicos |
| `Historico.razor` | `/historico` | — | `InteractiveServer` | Série de preços paginada + gráfico (JS interop) de um título |
| `Simulador.razor` | `/simulador` | — | `InteractiveServer` | Simula o rendimento de um investimento |
| `Cenarios.razor` | `/simulador/cenarios` | — | `InteractiveServer` | Compara múltiplos cenários de simulação |
| `Tributos.razor` | `/tributos` | — | `InteractiveServer` | Consulta/edita a configuração fiscal (IOF/IR) |
| `Docs.razor` | `/docs` | — (**pública**) | `InteractiveServer` | **Documentação da API** (tarefa 73): escrita à mão, no estilo `brapi.dev/docs` — nav lateral com âncoras, 9 endpoints de consumo com tabela de parâmetros, exemplos em cURL/C#/JS (`Shared/ExemploCodigo.razor`) e respostas JSON. Base URL vem de `ApiSettings:PublicBaseUrl` |
| `Desenvolvedores.razor` | `/desenvolvedores` | `@attribute [Authorize]` | `InteractiveServer` | **Portal do desenvolvedor** (tarefa 73): painel de credenciais (`Shared/ApiKeysPanel.razor`) + card apontando para `/docs` |
| `ApiKeys.razor` | `/minhas-api-keys` | — | estático (sem `@rendermode`) | Casca de redirect → `/desenvolvedores` (preserva links compartilhados; a tela real virou `Shared/ApiKeysPanel.razor`) |
| `Admin.razor` | `/admin` | `@attribute [Authorize(Roles = "Admin")]` | `InteractiveServer` | Gestão de usuários (pendentes/aprovar/desativar) |
| `About.razor` | `/about` | — | estático (sem `@rendermode`) | Perfil do autor |
| `Error.razor` | `/Error` | — | estático (sem `@rendermode`) | Página de erro padrão do template ASP.NET |

Só **duas** páginas exigem autenticação — `Desenvolvedores.razor` e `Admin.razor`; as demais são públicas (`ApiKeys.razor` virou casca de redirect na tarefa 73 e não exige auth: quem chega é mandado para `/desenvolvedores`, que exige). `Home`/`About`/`Error`/`ApiKeys` não têm `@rendermode` (estáticas); as outras 7 são `InteractiveServer`. Componentes compartilhados vivem em `Components/Shared/` (pasta criada na tarefa 73: `ApiKeysPanel.razor`, `GuiaIntegracao.razor`, `ExemploCodigo.razor`). A documentação (`/docs`) é **pública de propósito** — o integrador avalia o contrato antes de pedir chave. **Tema visual atual:** template genérico "dark finance" (`wwwroot/app.css`; `Components/App.razor` carrega a fonte Inter via Google Fonts CDN — `App.razor:9-11` — e o `chart.js` via `cdn.jsdelivr.net` — `App.razor:21`) → **tarefa 72** (mudança puramente de apresentação).

### Como se conecta

- **API → Application:** dois `IPipelineBehavior` registrados (ordem = mais externo primeiro): `LoggingBehavior` (Application, tarefa O3 — loga início/fim e captura `Result.IsFailure` como Warning via a interface `IResult` que `Result`/`Result<T>` implementam; nunca lança) e depois `CacheInvalidationBehavior` (Infra — invalida cache em comandos de escrita, testando `response is IResult r && r.IsSuccess`). **Não há ValidationBehavior/FluentValidation** — validação vive em cada handler, devolvendo `Result` de falha.
- **Web → API:** 100% HTTP, autenticação por `X-Api-Key` compartilhado (Web `ApiSettings:ApiKey` == API `ApiKey:Key`). Blazor server-side → chamada sai do servidor, navegador nunca vê a chave; sem CORS.
- **Contrato de erro:** falha devolve `{ Code, Description }`; cada página desserializa num record local `ApiError`.

### Fragilidades
- **Contratos vazando/duplicados** ✔️ RESOLVIDO parcialmente (tarefa 15): `POST /configuracoes/tributos` agora binda `CreateTributoRequest` (`API/Contracts/`), não mais o `CreateTributoCommand` da Application. Resíduo: `Create/UpdateTributoRequest` ainda referenciam `FaixaDto` da Application — movido para "Fora de escopo por ora" no PLANO. **Lado Web** ✔️ RESOLVIDO (tarefa 46, 2026-08-04): os DTOs de resposta eram `record`s privados duplicados/divergentes nas 5 páginas `.razor` (4 cópias de `TituloItem`; `SimulacaoResult` descartava `ProjecaoUtilizada`; `ApiError` lia `Description` em vez de `detail`). Consolidados em `Web/Contracts/` públicos, drops consertados, e um `ContractParityTests` (Web⊆API, reflexão) agora falha se um DTO do Web divergir do contrato da API — fim da divergência silenciosa.
- **Sem camada de acesso no Web** ✔️ RESOLVIDO (tarefa 16, 2026-07-25): era `Web/Services/` vazio com lógica de acesso à API (CreateClient, request anônimo, desserialização, `ApiError`) **duplicada nas 5 páginas**. **Corrigido:** typed client `TesouroApiClient` em `Web/Services/` (16a) + migração incremental das 5 páginas, uma por commit com E2E completo verde como gate (16b Titulos, 16c Historico, 16d Tributos, 16e Simulador, 16f Cenarios), removendo os 3 `record ApiError` duplicados e o client nomeado `"TesouroDiretoApi"`. Comportamento preservado 1:1 (E2E 22/22).
- **Enums só numéricos no JSON** ✔️ RESOLVIDO (tarefa 4): `JsonStringEnumConverter` registrado (aceita string E número); `ParseEnum` hardcoded removido do `Tributos.razor`.
- **Erro por string-matching** ✔️ RESOLVIDO (tarefa 7, refinado na tarefa 48): o `Error.Code.Contains("NotFound")` do PUT foi eliminado pelo helper `ToHttpResult`; a **tarefa 48 (2026-08-04)** removeu também o `EndsWith(".NotFound")` residual — o mapeamento agora é por **categoria explícita** (`enum ErrorType` no `Error`), sem qualquer parsing de código.
- **Sem validação declarativa na borda** — exception handler global ✔️ RESOLVIDO (tarefa 6/O4): `UseExceptionHandler` + `AddProblemDetails` → exceção crua vira `application/problem+json` com `correlationId`/`traceId`; 401 do `ApiKeyMiddleware` também padronizado.
- **Rate limiting** ✔️ RESOLVIDO (tarefas 66/67, 2026-08-08): eram endpoints **sem qualquer limite** de app (inclui `POST /importacao`, que dispara download/parse externo). **Corrigido:** rate limit **por cliente** (66, 60/min) + limiter **por IP nas falhas de auth** (67), ambos 429 problem+json + `Retry-After` — ver **§6 (Rate limiting)**. **Versionamento — ADR decidido (tarefa 57, 2026-08-08):** `/v1` no **path** via `app.MapGroup("/v1")` (não header), migração **big-bang** (sem cliente externo integrado), depois da auth; `_links`/`Location` herdam o prefixo do `LinkGenerator`, 2 hrefs hardcoded exigem edição manual; health/metrics/swagger ficam **fora** do `/v1`. ADR em [`docs/arch/ADR-versionamento.md`](./arch/ADR-versionamento.md); **execução concluída (tarefas 69–71, 2026-08-08):** big-bang API+Web na `main` (PR #45) e **verificado ponta a ponta na 71** (E2E 23 passed incl. `health` sem edição, smoke de login por mutação, ETag `/v1` miss→304, sem alias legado); borda nginx **não** precisou de mudança (opaca ao path após `/api/`) — smoke da borda + login Google real ficam como checklist manual do dono pós-deploy. **OpenAPI/Swagger** (tarefa 33, revisto na 37): `UseSwagger()`/`UseSwaggerUI()` são **incondicionais** (`Program.cs:29-30`) e `/swagger` entrou em `ApiKey:ExcludedPaths` — a UI e o `swagger.json` sobem em **todo** ambiente, **sem** `X-Api-Key`; o fechamento passou a ser na **borda** (`location /api/swagger { allow 127.0.0.1; deny all; }`), acessível só por túnel SSH. `GET /` retorna `"Hello World!"`; `AllowedHosts: "*"`.
- **Mapeamento `Result`→HTTP** ✔️ RESOLVIDO (tarefa 7, evoluído na tarefa 48): helper `ToHttpResult` em `Extensions/ResultExtensions.cs`, aplicado nos 4 `Endpoints/*.cs` — fim do `Contains("NotFound")` e do boilerplate por endpoint. **Tarefa 48 (2026-08-04):** o mapa deixou de ser por sufixo de string e passou a ser por **categoria explícita** no `Error` (`enum ErrorType`: `Validation`→400, `NotFound`→404, `Conflict`→409) — `EndsWith(".NotFound")` eliminado; `AlreadyExists`→409, com duplicata de tributo alcançável (`AnyAsync` no `TributoWriteRepository`, backstop no índice único `ix_tributos_nome`).
- **Sem superfície de documentação para integrador externo** — o Swagger UI existe (`UseSwagger()`/`UseSwaggerUI()` incondicionais, `Program.cs:29-30`, doc em `/swagger/v1/swagger.json`, isento de key via `ApiKey:ExcludedPaths`), mas é alcançável **só por túnel SSH**: o nginx fecha a rota (`location /api/swagger { allow 127.0.0.1; deny all; }`, decisão da tarefa 37). A tela `/minhas-api-keys` (tarefa 65) entregava credencial **sem** entregar contrato — quem recebia uma client key `td_…` não tinha como descobrir sozinho as rotas, o rate limit, o ETag ou a paginação. → **FECHADO pela tarefa 73**: o bloqueio de nginx **continua intocado** e a API segue sem Swagger público. A referência agora é **documentação escrita à mão** em `/docs` (`Components/Pages/Docs.razor`), **pública** e servida pelo Web — nav lateral com âncoras, 9 endpoints de consumo com tabela de parâmetros, exemplos em cURL/C#/JS e respostas JSON, mais seções de autenticação, limites, cache, paginação e erros. Rotas de manutenção (`/importacao`, POST/PUT de `/configuracoes/tributos`) ficam **fora** da doc por decisão do dono. Uma tentativa anterior desta tarefa embutiu o Swagger UI num `<iframe>` alimentado por um `openapi.json` filtrado; foi **descartada a pedido do dono** em favor da doc escrita — o pacote `Swashbuckle.AspNetCore.SwaggerUI`, o `OpenApiDocumentService` e o endpoint `/desenvolvedores/openapi.json` foram removidos do Web.

---

## 2. Modelo de Dados

### O que existe

**Domínio** (`src/TesouroDireto.Domain`), por contexto, tudo herdando `Common/Entity.cs` (igualdade por `Id`) e usando Result Pattern (`Common/Result.cs`, `Error.cs`, `DomainErrors.cs`):

- **Titulos** — agregado `Titulo` (`Entity<Guid>`, factory `Create`, ctor privado). VOs: `TipoTitulo` (8 instâncias), `Indexador` (whitelist de 4), `DataVencimento`. `Titulo` deriva `Indexador` e `PagaJurosSemestrais` do `TipoTitulo` (via pattern matching, `DeriveIndexador`).
- **PrecosTaxas** — agregado `PrecoTaxa` (referencia `Titulo` por `TituloId`, não navegação). VOs nuláveis: `Taxa`, `PrecoUnitario` (exige `> 0`), `DataBase`. Todos os 6 campos financeiros opcionais.
- **Tributos** — agregado rico `Tributo` com coleção encapsulada `_faixas`, métodos `Ativar/Desativar/AtualizarFaixas`. VO `Faixa` (record: DiasMin/DiasMax/Dia/Aliquota). Enums `BaseCalculo`, `TipoCalculo`. Único agregado com ctor sem parâmetros para EF.
- **Feriados** — `Feriado` + VO `DataFeriado`.
- **Simulador / DiasUteis** — objetos de cálculo transitórios, **não persistidos** (sem DbSet).
- **Usuarios / ApiKeys** (tarefa 59) — agregados de identidade: `Usuario` (`Entity<Guid>`, `google_sub` nullable até o 1º login, VO `Email`, enum `PapelUsuario`, campos de aprovação/auditoria `aprovado`/`aprovado_em`/`aprovado_por`) e `ApiKey` (agregado próprio, FK `dono_usuario_id`, VO `ApiKeyHash` = SHA-256 hex da key gerada em `FromRawKey` — **só o hash e o `prefixo` não-secreto** persistidos, nunca a key em claro). Base da onda de auth (56); **seed de admin feito (60)**; **middleware v2 feito (61)**; **gestão admin sobre usuários feita (62)** — `sync`/pendentes/aprovar/desativar com coluna `ativo` (default true) + `AdminOnlyFilter`; **self-service de keys feito (63)** — `GET/POST /me/keys` + `POST /me/keys/{id}/revogar` sob `UsuarioAprovadoFilter`, gerador CSPRNG (`td_`+Base64Url, só `hash`+`prefixo` persistidos, texto puro 1×), revogar liga o `InvalidateApiKeys()` do `CacheInvalidationBehavior`. **Endurecimento de confiança (63):** endpoints de gestão (`/me/*`, `/admin/*`) exigem **service key** (o `ApiKeyMiddleware` marca `service`/`client` em `HttpContext.Items`; filtros usam `IsServiceIdentity()`), pois `X-Acting-User-Sub` sozinho é forjável por qualquer client key — fecha vetor de personificação que também afetava a 62.

**Persistência** (`src/TesouroDireto.Infrastructure/Persistence`): `AppDbContext` implementa `IUnitOfWork`; 6 DbSets (Titulos, PrecosTaxas, Tributos, Feriados, Usuarios, ApiKeys — as 2 últimas na tarefa 59). Tabelas snake_case:
- `titulos` (PK uuid `ValueGeneratedNever`; índice único `ix_titulos_tipo_vencimento` em tipo+vencimento; desde a tarefa 12: `ix_titulos_data_vencimento` para filtro "vencido" e `ix_titulos_nome_upper` — índice funcional que torna `GetByNomeAsync` sargável). Desde a **tarefa 36**: identificador público `codigo` (slug tipo+data completa, ex. `tesouro-selic-2029-03-01`) **derivado na leitura** por `TituloCodigo.From` — **não persistido**, sem coluna nem migration; o lookup reverso (`GetByCodigoAsync`) filtra por `data_vencimento` (reusa `ix_titulos_data_vencimento`) e casa o slug em memória. O uuid `id` é chave sintética interna, agora marcada DEPRECATED nos contratos (extinção na tarefa 38).
- `precos_taxas` (FK `titulo_id` CASCADE; índice único `ix_precos_taxas_titulo_data`; valores numeric nuláveis)
- `tributos` + `tributo_faixas` (`OwnsMany`; índice único `ix_tributos_nome`)
- `feriados` (índice único `ix_feriados_data`)
- `usuarios` (tarefa 59; PK uuid `ValueGeneratedNever`; uniques `ix_usuarios_google_sub` e `ix_usuarios_email`; self-FK `aprovado_por`→`usuarios` `Restrict` com `ix_usuarios_aprovado_por`; `papel` via `HasConversion<string>`)
- `api_keys` (tarefa 59; unique `ix_api_keys_hash`; FK `dono_usuario_id`→`usuarios` `Restrict` com `ix_api_keys_dono_usuario_id`; `hash` via `HasConversion` do VO `ApiKeyHash`; `ultimo_uso_em` nullable — gravação com throttle fica para a tarefa 61)

4 migrations (`InitialCreate`, `MakePrecoTaxaValuesNullable`, `AddFeriados`, `AddUsuarioApiKey`) + snapshot alinhado (sem drift aparente). A `AddUsuarioApiKey` (tarefa 59) é **aditiva** e reversível (`Down` dropa `api_keys` antes de `usuarios`); aplicada em Postgres real e round-trip dos repos **confirmados no CI** (Testcontainers). Leitura Dapper de `timestamptz` exige o `DateTimeOffsetTypeHandler` em `DapperTypeHandlers` (Npgsql devolve `DateTime`).

### Como se conecta

- **Dapper (read → DTO):** `TituloReadRepository`, `PrecoTaxaReadRepository`, `FeriadoReadRepository` — SQL cru, `NpgsqlDataSource` singleton, `DateOnlyTypeHandler` custom. Todos embrulhados por decorators de cache.
- **EF Core (write):** `*WriteRepository` (Add/Update/Exists); `SaveChanges` só via `IUnitOfWork`.
- **Exceção ao padrão:** `TributoReadRepository` usa **EF e devolve a entidade de domínio** (não DTO) — Tributo é agregado com coleção owned consumida pelo Simulador.
- **Mapeamento de VOs:** `HasConversion` nas configs — VOs → string/decimal via `.Value` na materialização; `Faixas` como `OwnsMany`; enums como string.

### Fragilidades
- **VO `Indexador` com whitelist rígida quebra materialização EF** ✔️ RESOLVIDO (tarefa 2, 2026-07-19): novo factory `Indexador.FromPersistence(string)` (lossless, sem-falha) usado só na leitura do EF (`TituloConfiguration`); `FromName` permanece estrito para validação de filtro (contrato de API intacto). Valor fora da whitelist na coluna não quebra mais a materialização. `TipoTitulo` já era mitigado por pattern matching. **Resíduo (tarefa 22):** a coluna `indexador` segue `HasMaxLength(20)` — a leitura ficou tolerante, mas um valor bruto >20 chars na escrita ainda truncaria/estouraria.
- **Converters `.Value` sem tratamento de falha**: `DataVencimento/DataBase/PrecoUnitario.Create(v).Value` assumem sucesso; um `pu` ≤ 0 no banco faria a materialização EF lançar (hoje mascarado porque reads passam por Dapper). **Parcial** ✔️ (tarefa 42, 2026-07-28): o call-site do **import** (`ImportCsvCommandHandler.CreatePuOrNull`) parou de abortar o lote por PU negativo (linha suja = `linhasComErro++`+log, lote segue); o caminho **EF/converter de materialização** do mesmo anti-padrão **segue aberto**.
- **VO `Taxa` sem invariante** ✔️ RESOLVIDO (tarefa 45, 2026-07-28): era `Create` devolvendo `Taxa` direto (não `Result`), aceitando qualquer decimal — inconsistente com os demais VOs e raiz do anti-padrão `.Value` do import (tarefa 42). **Corrigido:** `Taxa.Create`→`Result<Taxa>` com invariante **sinal-tolerante** (rejeita só `value == 0`, o sentinela de "sem taxa"; negativo é **válido** — Selic com deságio no CSV do Tesouro Transparente); call-sites propagam `Result` (import faz early-return da falha, EF read usa `.Value` simétrico a `PrecoUnitario`/`DataBase`). Reject `<=0`/`<0` descartados de propósito para não descartar linhas válidas no job das 06:00.
- **Invariantes fiscais não protegidas**: `Faixa`/`Tributo` não impedem faixas sobrepostas/lacunas na tabela regressiva — corretude depende do seed correto.
- **Redundância em `Titulo`**: `indexador`/`paga_juros_semestrais` derivados **e** persistidos; import parcial pode divergir do que o ctor derivaria, sem check de consistência.
- **Índices ausentes para filtros comuns** ✔️ RESOLVIDO (tarefa 12, 2026-07-23): filtro por `data_vencimento` isolado (vencido) varria tabela e `GetByNomeAsync` usava expressão não-sargável (`UPPER(... || EXTRACT(YEAR...))`). **Corrigido:** migration `AddTituloIndexes` — `ix_titulos_data_vencimento` (btree, modelado no EF) + `ix_titulos_nome_upper` (índice funcional via `migrationBuilder.Sql`, expressão idêntica à de `GetByNomeAsync`). EXPLAIN no banco real migrado (402 títulos): `GetByNomeAsync` → Index Scan (22ms→0.08ms em lab de 50k); `GetFilteredAsync vencido=true` (14%) → Index Scan; `vencido=false` (86%) → Seq Scan (escolha correta do planner). **Índice em `indexador` avaliado e REJEITADO**: 4 valores distintos, seletividade ~25%, ganho de só 2× em 50k linhas — não justifica o custo de escrita.
- **Desvio CQRS**: `TributoReadRepository` devolve entidade em vez de DTO (contraria a convenção do projeto).

---

## 3. Integrações Externas

### O que existe (4 integrações, registradas em `Infrastructure/DependencyInjection.cs`)

1. **Projeções / BCB Focus** — `FocusBcbService` (`IProjecaoMercadoService`). API Olinda do BCB (OData), `FocusBcb:BaseUrl`. Dois endpoints: `ExpectativasMercadoSelic` e `ExpectativasMercadoInflacao12Meses` (`$filter=Indicador eq '...'`, mapeia IGPM→`IGP-M`). `$top=1&$orderby=Data desc`. Typed client, timeout **30s**. `Prefixado` rejeitado antes de chamar.
2. **Feriados / ANBIMA** — `FeriadoImportService` (`IFeriadoImportService`, `IAsyncEnumerable`). XLS binário (BIFF) via `ExcelDataReader`, `FeriadoImport:Url`. Col 0 = data, col 2 = descrição; baixa o arquivo inteiro para `MemoryStream`. Typed client, timeout **5min**. Agendado por **Quartz** desde a tarefa 10 (`FeriadoImportJob`, cron `Feriados:CronSchedule`, fallback `0 0 6 1 12 ?`, `[DisallowConcurrentExecution]`), além do endpoint manual `POST /importacao/feriados`.
3. **CSV Tesouro Direto** — `CsvImportService` (`ICsvImportService`, `IAsyncEnumerable<Result<CsvRecord>>`). Tesouro Transparente/CKAN, `CsvImport:Url`. CSV `;`, 8 colunas, decimais `pt-BR`, `dd/MM/yyyy`. `StripTrailingYear` remove ano do nome via regex. Streaming linha-a-linha. Typed client, timeout **10min**. Agendado por **Quartz** (cron `0 0 6 * * ?`, `[DisallowConcurrentExecution]`).
4. **Caching** — decorators cache-aside com `IMemoryCache`: `CachedTituloReadRepository` (24h), `CachedPrecoTaxaReadRepository` (6h), `CachedTributoReadRepository` (24h), `CachedFeriadoReadRepository` (7d) — desde a tarefa 53 (2026-08-05) os quatro passam pelo helper único `MemoryCacheResultExtensions.GetOrCreateResultAsync` (só cacheia em sucesso, chaves/tokens de invalidação preservados) e leem o TTL de `appsettings.json` seção `Caching:*` (`?? DefaultTtl` em código = valor antigo, então config ausente = comportamento atual) — e, desde a tarefa 11 (2026-07-23), `CachedProjecaoMercadoService` — por indexador, entrada "fresh" (TTL 6h, `FocusBcb:CacheTtl`) e entrada "lkg"/last-known-good (7 dias, `FocusBcb:MaxFallbackAge`); fallback só em `Projecao.HttpError`, nunca silencioso (`LogWarning` + campo `Origem`=`Bcb`/`CacheFallback`, `ObtidaEmUtc` original preservado). Essa composição de DI (`IProjecaoMercadoService` → `CachedProjecaoMercadoService`, com `FocusBcbService` como colaborador interno de `Timeout=30s`) é **travada por teste** desde a tarefa 23 (2026-07-25, `DependencyInjectionCompositionTests`): uma regressão para `AddHttpClient<IProjecaoMercadoService, FocusBcbService>` — que removeria cache/fallback do caminho de produção e faria a suíte bater na API real do BCB — deixa o teste vermelho. Invalidação dos 4 primeiros disparada pelo `CacheInvalidationBehavior` (pipeline MediatR, `switch` por tipo de comando) via `MemoryCacheInvalidator`. O 5º token (`GetProjecoesToken`/`InvalidateProjecoes`) existe no invalidator e é usado pelo `AddExpirationToken` do `CachedProjecaoMercadoService`, mas **nenhum comando o invalida hoje** — `InvalidateProjecoes()` só é chamado em `ApiTestFactory.cs` (reset entre testes); em produção a expiração das entradas de projeção depende só do TTL/absoluto (fresh 6h / lkg 7d). As três invariantes de fallback do decorator estão **travadas por teste** desde a tarefa 26-28 (2026-07-26): (i) `Projecao.NotFound` com `lkg` quente propaga 404 `problem+json` **sem** servir o fallback (integração), (ii) cancelamento do chamador propaga `OperationCanceledException` enquanto o cancelamento interno/timeout vira `HttpError` (unitário), (iii) expiração de TTL exercitada por avanço de relógio fake (`FakeTimeProvider` injetado no decorator via `ApiTestFactory`, sem `Task.Delay`).

### Como se conecta

- **Ingestão:** CSV (Quartz 06:00) → `ImportCsvCommand` → handler grava `Titulo`/`PrecoTaxa` em lotes de 1000, deduplica por `DataBase`; sucesso invalida cache titulos+precos. Feriados (Quartz 1º/dez 06:00, `FeriadoImportJob`, tarefa 10, ou `POST /importacao/feriados` manual) → `ImportFeriadosCommand` → handler grava, deduplica; invalida cache feriados.
- **Consumo:** `SimularCommandHandler` chama `IProjecaoMercadoService.GetProjecaoAsync` (quando `ProjecaoAnual` ausente e não-Prefixado), usa `MedianaAnual`; desde a tarefa 11 (2026-07-23) essa chamada resolve para `CachedProjecaoMercadoService` (cache fresh 6h + fallback last-known-good 7 dias por indexador, `FocusBcbService` como colaborador interno) em vez de bater ao vivo no BCB a cada simulação; usa `IDiasUteisService` → `FeriadoReadRepository` (cacheado) → `DiasUteisCalculator`. Desde a tarefa 47 (2026-08-04) o port `IDiasUteisService` devolve `Result<DiasUteisFeriados>` numa **leitura única** de feriados (os handlers pararam de reler `GetAllDatasAsync`), e desde a tarefa 52 (2026-08-05) `SimularCommandHandler` e `SimularCenariosCommandHandler` compartilham o núcleo em `SimuladorApplicationService` (carrega contexto + monta `SimulacaoInput` + mapeia DTO num ponto único); o de cenários passou a instrumentar as métricas O8 por cenário (mesmos labels `indexador`+`outcome`, cardinalidade inalterada).
- **Camadas:** ports em Application, adapters em Infrastructure, cálculo puro em Domain. Cache é infra pura via pipeline behavior.

### Fragilidades
- **Resiliência ausente** ✔️ RESOLVIDO (tarefa 13, 2026-07-25): Era: nenhum typed client tinha Polly/retry/circuit breaker — só timeout; indisponibilidade transitória de BCB/ANBIMA/Tesouro falhava a operação inteira. **Corrigido:** `AddResilienceHandler` (`Microsoft.Extensions.Http.Resilience`) nos 3 clients — retry+backoff+attempt-timeout nos 3, mais circuit breaker **só** no `FocusBcbService` (nos batch CSV/feriados o CB seria config morta: chamados 1×/dia e 1×/ano nunca acumulam amostras na janela). Timeouts aninhados dentro do `HttpClient.Timeout` de cada client (BCB total 25s < 30s; CSV/feriados ≈194s < 600s/300s). O `catch` dos dois imports foi alargado para o `TimeoutRejectedException` do Polly (só no caminho até-headers, dentro do `GetAsync`) — a fragilidade separada "exceção durante o **streaming do corpo** (após headers OK)" continua aberta.
- **BCB Focus sem cache e sem fallback** ✔️ RESOLVIDO (tarefa 11, 2026-07-23): Era: `IProjecaoMercadoService` **não** era embrulhado por cache — cada simulação sem `ProjecaoAnual` era 1 chamada ao vivo; BCB fora → simulação inteira falhava; acoplava disponibilidade do simulador à do BCB e não escalava (N simulações = N chamadas). **Corrigido:** `CachedProjecaoMercadoService` decora `IProjecaoMercadoService` — entrada "fresh" por indexador (TTL 6h) evita bater no BCB a cada simulação; entrada "lkg" (7 dias) serve de fallback só quando o BCB devolve `Projecao.HttpError`, nunca silencioso (log + campo `Origem` na resposta). Limitação aceita: `lkg` em memória, zera em restart/deploy.
- **Parsing frágil**: `DateOnly.Parse(entry.Data)` sem cultura explícita; indicador de inflação por igualdade de enum (novo indexador → `$filter` vazio, NotFound silencioso); `FeriadoImportService` depende de posições fixas de coluna e formato BIFF (migração para `.xlsx` quebra); `CsvParserHelper` exige 8 colunas/`pt-BR` fixos, `StripTrailingYear` mutila título terminando em 4 dígitos.
- **Robustez de rede/memória**: XLS inteiro em `MemoryStream` sem limite; exceção durante streaming do corpo (após headers OK) não capturada; desserialização do BCB sem try/catch (JSON malformado → `JsonException` crua).
- **Cache em processo (não distribuído)**: em deploy multi-instância a invalidação por import só limpa a instância que processou; outras servem dados velhos até o TTL. Invalidação depende do `switch` manual no behavior — novo comando de escrita não adicionado deixa cache stale sem teste de guarda.

---

## 4. Jobs / Crons, Startup e Observabilidade

### O que existe

**Jobs (Quartz.NET):** dois jobs registrados. `CsvImportJob` — cron `0 0 6 * * ?` (06:00 diário, configurável), `[DisallowConcurrentExecution]`, `WaitForJobsToComplete = true`. Dispara `ImportCsvCommand` e loga resultado. `FeriadoImportJob` ✔️ RESOLVIDO (tarefa 10, 2026-07-23) — cron `0 0 6 1 12 ?` (06:00 do dia 1º de dezembro, configurável em `Feriados:CronSchedule`), também `[DisallowConcurrentExecution]`, espelha o `CsvImportJob` linha a linha e dispara `ImportFeriadosCommand` (idempotente por dedup de datas). BCB Focus é sob demanda (com cache/fallback — ver §3, tarefa 11).

**Startup** (`API/Program.cs`): `ApiKeyGuard.Validate` aborta o boot em prod se `ApiKey:Key` for vazia ou uma chave proibida (`CHANGE-ME-IN-PRODUCTION` **ou `dev-local-key`** desde a tarefa 19; `Trim`+`OrdinalIgnoreCase`); o `docker-compose.yml` usa `${API_KEY:?}` (sem fallback inseguro). Migrations automáticas em todo ambiente exceto `Testing`. **Healthcheck real:** `AddDbContextCheck<AppDbContext>()` com `/health` + `/health/ready` (readiness, 503 se banco fora) e `/health/live` (liveness, não toca DB). **Seed no boot** (tarefa 9): `InitializeDatabaseAsync` (`API/Extensions/`) migra → semeia tributos IOF/IR via `SeedTributosCommand` idempotente (fonte `TributosPadrao`, validada pelo domínio; FATAL em falha) → importa feriados da ANBIMA só no 1º boot se a tabela estiver vazia (não-fatal) → **semeia o admin via `SeedAdminCommand` idempotente (tarefa 60, não-fatal): lê `ADMIN_EMAIL` da config; ausente/vazio ⇒ no-op logado (ADR §3.8); existe ⇒ garante `papel=Admin`+`aprovado=true` sem duplicar (via `Usuario.GarantirAdminAprovado`, `AprovadoPor` nulo); senão cria com `google_sub` nulo. `docker-compose.yml` usa `ADMIN_EMAIL=${ADMIN_EMAIL:?}` (obrigatório em prod) e o `deploy.yml` grava a var do secret `ADMIN_EMAIL`.** Guard de `Testing` (early-return). **Feito (tarefa 55, 2026-08-06):** o wiring de todas as camadas, antes concentrado no `Program.cs` (+ `Infrastructure/DependencyInjection.cs`), foi extraído para extension methods por camada — `AddApplication` (MediatR + `LoggingBehavior`), `AddInfrastructure(IConfiguration)`, `AddApiServices` (health/`AddDbContextCheck`, swagger, problem+json, JSON enum); `Program.cs` virou só orquestração. `AddApplication` é chamado **antes** de `AddInfrastructure` para preservar a ordem dos pipeline behaviors do MediatR (Logging→Metrics→Cache); refactor puro, comportamento e ordem de decorators/behaviors idênticos. Regra: serviço novo nasce no `DependencyInjection.cs` da sua camada, não no `Program.cs`.

**Observabilidade:**
- **Logs:** Serilog com **CompactJsonFormatter (CLEF) no Console E no sink `GrafanaLoki`** (tarefa O1 + formatter padronizado na tarefa 20), label `job=tesouro-direto-api` e enrichers `service`/`environment`/`MachineName` fixados no código (`API/Extensions/SerilogExtensions.cs`), enrich `FromLogContext`, URI Loki em `Loki:Uri` — desde a tarefa 77.2 aponta para `http://alloy:3100` (não mais um container `loki`, removido na 77.4; `docker-compose.yml:13,170`), e o Alloy recebe o push em claro só na rede interna (`loki.source.api`, porta 3100) e reenvia autenticado ao Grafana Cloud (`infra/alloy/config.alloy:211-222`) — mantém o token do Cloud num único lugar, sem tocar o sink Serilog em C#. Captura de falhas de negócio (sem exceção) via `LoggingBehavior` (MediatR, tarefa O3, em `Application/Common/Behaviors/`) → Warnings estruturados. O campo `level` (`info`/`warning`/`error`) é injetado pelo próprio sink Loki. (O diretório `Infrastructure/Observability` citado na tarefa **não existe**.)
- **Métricas:** `prometheus-net` em `/metrics`: HTTP server genérico (`UseHttpMetrics`, envolto em `UseWhen` que **exclui `/health*` e `/metrics`** via reuso de `ApiKey:ExcludedPaths` — tarefa 29, 2026-07-25 — para não inflar o denominador da regra de 5xx; label real da série é `endpoint`, não `http_route`), **latência/desfecho por caso de uso** via `MetricsBehavior` (MediatR — `mediatr_request_duration_seconds`/`mediatr_requests_total`, tarefa O6, 2026-07-23) e **latência/erro por dependência externa** via `.UseHttpClientMetrics()` nos 3 typed clients (`httpclient_request_duration_seconds` rotulado por `client`, tarefa O7, 2026-07-23). **Métricas de negócio** ✔️ RESOLVIDAS (tarefas 14/O8, 2026-07-24): 6 séries via porta `IBusinessMetrics` — `import_last_success_timestamp_seconds`, `import_runs_total{outcome}`, `simulations_total{indexador,outcome}`, `simulation_failures_total{reason}`, `import_prices_processed_total{kind}`, `tributos_config_changes_total{op}` — provadas em `/metrics` por teste de integração; painel de frescor no Grafana. Scrape de `app:8080` (job `tesouro-direto-api`) hoje é feito pelo **Grafana Alloy** (`prometheus.scrape "app"`, `infra/alloy/config.alloy:16-22`), que reenvia por `remote_write` ao Grafana Cloud — o `prometheus.yml` da raiz do repo não participa mais da observabilidade contínua: desde a 77.4 serve **só** ao Prometheus efêmero do `profiles: ["load"]`, usado pelo k6 durante teste de carga (`prometheus.yml:17-21`; ver §5/§7). **O próprio Alloy também é scrapeado, desde a tarefa 80.1 (2026-08-16):** `discovery.relabel "alloy"` sobre `127.0.0.1:12345` força `job="alloy"` e `prometheus.scrape "alloy"` (30s) reenvia pelo mesmo `remote_write` — mesma técnica que a 77.1 usou para `job="node"`. Existia como buraco antes disso: nenhuma série `go_memstats_*`/`alloy_*` chegava à nuvem, então diagnosticar o consumo de memória do coletor exigia sessão SSH manual (ver tarefa 80, PLANO). Motivo do buraco não ser detectado antes: o `AlloyContractTests` que cobre a fiação de relabel nasceu vácuo para quebra completa da cadeia (fechado na 80.1 — ver PLANO).
- **Grafana / Loki / Prometheus locais:** não existem mais. A tarefa 77 (2026-08-15) removeu os 4 containers de observabilidade local que a O9/74/76 tinham provisionado (`grafana`, `loki`, `promtail`, `node-exporter`, 516 MB de teto somado — 77.4) e migrou alerting e dashboards para o **Grafana Cloud** (free tier). O diretório `infra/grafana/provisioning/` que hospedava o antigo provisioning **não existe mais no repo**. **Alertas:** **21 regras** Grafana-managed **na nuvem**, versionadas em `infra/grafana/cloud/rules.yaml` (+ `contactpoints.yaml`, `policies.yaml`; confirmado por leitura — `grep -c "^      - uid:" infra/grafana/cloud/rules.yaml` devolve **21** `uid:` distintos), aplicadas por `scripts/grafana-cloud/apply-cloud.sh` (a API de provisioning do Cloud não aceita o formato de arquivo do `/export`, então o script decompõe cada YAML na forma que a API realmente aceita, confirmada por sondagem HTTP contra a stack real) → contact point **Telegram**, o mesmo destino desde a O9. As regras cobrem frescor (gate dia+hora BRT), app down, DB/readiness, 5xx>5%, p95>1.5s, falha de import, simulador BCB, rate-limit anômalo e disco cheio, mais as **8 regras de recurso por container** da tarefa 76 (ver fragilidade em §7), mais **3 regras da 77.5** sobre a saúde da própria ingestão do Grafana Cloud (séries ativas perto do teto do free tier, overage de métricas/logs, projeção mensal de logs perto do teto) — lidas de `grafanacloud-usage`, um datasource **diferente** do `grafanacloud-prom` que as outras 18 usam (é o datasource de billing/uso da própria conta Grafana Cloud, não o remote_write da aplicação); existem porque, se a ingestão passar a ser rejeitada, as outras 18 regras ficam mudas sem aviso. **Dashboards:** `infra/grafana/dashboards/tesouro-direto.json` e `host.json` sobem para a nuvem pelo mesmo `apply-cloud.sh`; `load-test.json` **não sobe por design** (lê o Prometheus efêmero local do `profiles: ["load"]`, inalcançável pelo backend SaaS) — subiu por engano na 77.3 e o script hoje o **apaga** da nuvem por convergência (`scripts/grafana-cloud/apply-cloud.sh:184-203`). **Retenção do free tier:** 14 dias de métricas, 10k séries ativas, 50 GB de log/mês, 500 regras — estourar **rejeita ingestão**, não gera fatura. **Cardinalidade medida (2026-08-16, após a 80.1):** 675 séries ativas (`node` 263 + `tesouro-direto-api` 412) antes do self-scrape do Alloy → **1409** depois (`14%` do teto de 10k), delta atribuível +640 ao novo `job="alloy"`. Achado histórico preservado: o campo `chatid` do provisioning local **não aceitava** env interpolada — corrigido com string literal (hotfix pré-77; ver `feedback_grafana_chatid_string_literal`); irrelevante hoje porque o provisioning local não existe mais, mas fica registrado porque o achado em si (envs não interpoladas em `chatid`) pode se repetir em qualquer outro provisioning YAML.
- **Web (Blazor):** ✔️ RESOLVIDO (tarefa 17/O2) — Serilog + sink Loki (CLEF, `job=tesouro-direto-web`) + `CorrelationIdHandler` (`DelegatingHandler`) propagando `X-Correlation-Id` à API → trace Web↔API no Grafana. Métricas do Web ainda ausentes.

**Deploy** (`.github/workflows/deploy.yml`): `test` → `e2e` → `deploy` (SSH VPS). `.env` sempre reescrito com `printf`, `git fetch` + `git reset --hard origin/main` (não `git pull`), copia nginx conf, `nginx -t && reload`, `docker compose build` (sem `--no-cache` desde a tarefa 74.0/PR #59). Depois do build, três comandos em sequência — reescritos na 77.4 e comentados com o achado que motivou cada um: `docker compose up -d --remove-orphans` (`deploy.yml:236`; sem `--remove-orphans` os 4 containers de observabilidade removidos do arquivo nesta fase ficariam de pé, fechando o deploy VERDE sem liberar nada), `docker compose --profile load rm -sf prometheus` (`deploy.yml:253`; limpa o Prometheus efêmero que um `run-load.sh --local` anterior possa ter deixado de pé — um serviço sob `profiles:` **não** é órfão, então o `--remove-orphans` acima não o alcança), e `docker compose up -d --force-recreate --no-deps alloy` (`deploy.yml:257`; `infra/alloy/config.alloy` é bind-mount, e `up -d` não recria um serviço só porque o conteúdo do bind mudou). Aguarda `/health/ready`. Migrations rodam no startup do container. Nginx do host com TLS (tarefa 44, 2026-08-03): domínio `dadosdotesourodireto.com.br`, cert Let's Encrypt (Certbot `--standalone`→renovação `--webroot` + `certbot.timer`); `listen 80`→301 https (+ acme-challenge), `listen 443 ssl http2` com `Strict-Transport-Security: max-age=300`, `listen 3080` mantido como break-glass. `/`→Web, `/api/`→API; `/api/swagger` e `/api/metrics` restritos a 127.0.0.1 (`allow 127.0.0.1; deny all` — túnel SSH; `/api/metrics` fechado na tarefa 43, 2026-07-28). As rotas `/grafana/` e `/prometheus/` existiram até a 77.4 e **saíram** de `infra/nginx/tesouro-direto.conf` junto com os containers locais (confirmado: nenhuma ocorrência de `grafana`/`prometheus` no arquivo hoje) — o Grafana agora é o Grafana Cloud, alcançado direto pela nuvem, fora do nginx da VPS.

### Como se conecta

Quartz → `ImportCsvCommand` → handler → HTTP Tesouro + write repos (EF). CorrelationId flui header → LogContext → Serilog → Alloy (`loki.source.api`, porta 3100) → Grafana Cloud → derivedField no Grafana. Grafana Alloy faz scrape de `app:8080` (métricas) e tail de log (nginx/kernel/API/Web) → `remote_write`/`loki.write` para o Grafana Cloud, onde vivem os dashboards e as 21 regras de alerta (`infra/grafana/cloud/`, ver §7).

### Fragilidades
- **Sem seed de tributos (IOF/IR) e feriados em produção** ✔️ RESOLVIDO (tarefa 9, 2026-07-22): `InitializeDatabaseAsync` (`API/Extensions/DatabaseInitializerExtensions.cs`) no boot semeia tributos (idempotente, via domínio, FATAL em falha) e importa feriados da ANBIMA no 1º boot (não-fatal). Verificado ao vivo: banco novo → `POST /simulador` 200; revisão fiscal manual dos valores semeados OK (2026-07-23). Bug de cache stale (24h) achado e corrigido no caminho. Refresh contínuo de feriados entregue na tarefa 10 (`FeriadoImportJob`, ver Jobs acima). **Follow-ups da 9:** cobertura automatizada da semântica fatal/não-fatal do boot ✔️ RESOLVIDO (tarefa 21, 2026-07-23): lógica extraída para `IDatabaseInitializer` (`API/Extensions/DatabaseInitializer.cs`) com seam `IDatabaseMigrator`; 6 testes cobrem os caminhos (seed FATAL, feriados não-fatal, Testing pula, ramos de checagem/já-semeado), não-vacuosos por mutação; suíte 406/406 + boot real revalidado (tributos=2, feriados=1263, idêntico ao baseline). Corrida de dois pods no boot (`DbUpdateException` crua) → **fora de escopo** (efeito final correto; exigiria lock distribuído).
- **Feriados sem agendamento** ✔️ RESOLVIDO (tarefa 10, 2026-07-23): Era: só endpoint manual, sem Quartz — feriados do próximo ano não entrariam sozinhos → distorceria dias úteis → distorceria todas as projeções do Simulador. **Corrigido:** `FeriadoImportJob` registrado no Quartz (`JobKey("feriado-import")`, cron `Feriados:CronSchedule`, fallback `0 0 6 1 12 ?`), dispara `ImportFeriadosCommand`. **Achado do revisor:** a fonte ANBIMA (`feriados_nacionais.xls`) já publica feriados de 2001 até **2099** de uma vez — o boot da tarefa 9 já importa todos os anos futuros. O valor real deste job, portanto, **não** é "esperar o próximo ano ser publicado" (a premissa original acima), e sim resiliência a correções/adições pontuais da fonte ao longo do tempo.
- **Lock apenas intra-instância** ⚠️: `[DisallowConcurrentExecution]` + RAMJobStore (in-memory). Multi-instância ou cron concomitante a `POST /importacao` manual → sem lock distribuído. Idempotência real vem de `existingDates.Contains(DataBase)`, mas `GetOrCreateTitulo` não é atômico — depende da constraint única `ix_titulos_tipo_vencimento` (ver Verificação #6).
- **Job silencioso em falha** ✅: `CsvImportJob` só faz `LogError`; sem métrica, sem alerta, sem relançar. Fonte fora do ar por dias passa despercebida.
- **Sem métricas de negócio/job** ✅: nenhum counter/gauge para sucesso/falha do import, linhas processadas, idade do último preço, duração do job. Impossível alertar em Grafana.
- **Healthcheck raso** ✔️ RESOLVIDO (tarefa 3, 2026-07-19): `AddDbContextCheck` + `/health`/`/health/ready` (503 com banco fora) e `/health/live` (liveness); Docker healthcheck e gates de deploy apontam para `/health/ready`.
- **Web sem observabilidade** ✔️ RESOLVIDO (tarefa 17/O2, 2026-07-21): Serilog+Loki no Web + `CorrelationIdHandler` → logs e CorrelationId da UI entram no Loki/Grafana (trace Web↔API). Resíduo: métricas do Web (latência/erro de render) ainda ausentes.
- **Grafana exposto publicamente com senha default** ✔️ RESOLVIDO (tarefa 1 + commit `ba3b103`, 2026-07-19): `GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:?...}` (boot falha sem o secret, sem fallback `:-admin`) e `/grafana/` restrito por IP no nginx. **Superado pela tarefa 77 (2026-08-15):** o Grafana local (container, senha, rota `/grafana/`) deixou de existir por completo — o Grafana hoje é o Grafana Cloud (SaaS), com controle de acesso próprio, fora do escopo desta mitigação de borda.

---

## 5. Testes

### O que existe

6 projetos C# (**584 métodos** xUnit, verde em 2026-08-05: Domain 152 · Application 95 · Infrastructure 66 · Architecture 14 · Web/bUnit 31 · API 226) + E2E Playwright/TS (**23 testes**). Stack: xUnit 2.5.3, FluentAssertions 8.9, NSubstitute 5.3, coverlet; Testcontainers.PostgreSql + Mvc.Testing (só API.Tests).

- **Domain.Tests** — unitário puro: VOs, entidades, Result, `DiasUteisCalculator` (14), `SimuladorService` (16, maior concentração), `TipoTitulo` pattern matching (17).
- **Application.Tests** — handlers CQRS com repos mockados (NSubstitute): PrecosTaxas, Titulos, Tributos, Simulador, Importacao, Feriados. Inclui `Simulador/FiscalGoldenTests` (tarefa 49, 2026-07-29): 10 valores-ouro ponta-a-ponta sobre a config fiscal **real** (`TributosPadrao.Build()`) — exercita o ramo IOF `TabelaDiaria` e trava a interação IOF→IR — + 5 propriedades determinísticas (enumeração exaustiva de `DiasCorridos`, sem FsCheck). Cobre o cálculo fiscal; **não** cobre o caminho de cupom (lacuna registrada na tarefa 54).
- **API.Tests** — mistura: `Persistence/` (5 classes, **integração real Testcontainers postgres:16**, migrate + write/read repos); `Middleware/` (3 classes, `WebApplicationFactory` + connection string fake — ApiKey 7 testes, CorrelationId, métricas); `Projecoes/FocusBcbServiceTests` (HTTP mockado); `Feriados/FeriadoImportServiceTests` (.xls real embutido); `CsvImport/` (parser + job).
- **Infrastructure.Tests** — cobre **apenas** `Caching/` (decorators, invalidator, behavior).
- **Architecture.Tests** — impõe direção de dependências, convenções de código (handlers ctor único, commands records, Application sealed, repos retornam `Task<Result>`) e convenções de Domain (sealed, sem `List<>` exposto).
- **E2E.Tests** — Playwright: projeto `api` (health/metrics) + `web` (5 páginas), `retries: 2`, `workers: 1`, helpers com espera de `window.Blazor`.

### Como se conecta

**CI gateia deploy:** `test` (dotnet test + cobertura opencover + **gate de cobertura** que reprova o job se a linha cair abaixo de **94%** — tarefa 18, via `scripts/coverage-gate.py` sobre o opencover; mede API/Application/Domain/Infrastructure, **não** o Web — + SonarQube condicional; tem service container postgres 5432 **aparentemente não consumido**) → `e2e` (`docker-compose.e2e.yml` + seed.sql via psql + Playwright chromium; E2E vermelho bloqueia deploy) → `deploy` (SSH). Ambiente E2E efêmero (db postgres:16 + app + web). `seed.sql` idempotente (TRUNCATE+INSERT): 7 títulos, preços, IOF+IR, feriados 2024-2025 — independente das migrations. Testes de integração de Infrastructure moram fisicamente em **API.Tests**, não em Infrastructure.Tests.

### Fragilidades
- **Zero teste de integração HTTP nas 11 rotas de negócio** ✅ (Verificação #10): `WebApplicationFactory` só nos 3 testes de middleware (batem em `/` e `/health`). Rotas reais cobertas só por handlers unitários (repos mockados) + E2E via Blazor. Bug de roteamento/serialização (ex.: enums numéricos) passaria despercebido.
- **E2E web baseado em presença de elementos**: muitos testes só `toBeVisible()`/contagem (contraria diretriz de testes comportamentais). Há exceções boas (simulador valida `R$`, tributos cria real, titulos filtra).
- **Flakiness inerente do Blazor Server**: `retries: 2` + hacks `waitForTimeout(1000)` "if Blazor missed the event".
- **Adapters de I/O sem integração real**: FocusBcb só com `FakeHttpMessageHandler`; `CsvImportService` **sem teste dedicado** (só mockado no handler).
- **Infrastructure.Tests só cobre Caching**; resto testado a partir de API.Tests (placement inconsistente).
- **Sem gate de cobertura** ✔️ RESOLVIDO (tarefa 18, 2026-07-25): `scripts/coverage-gate.py` parseia o opencover já produzido e **reprova o job `test`** se a linha cair abaixo do threshold (= floor do medido; sem ferramenta nova). **Testes de componente Blazor (bUnit)** ✔️ RESOLVIDO (tarefa 32, 2026-07-26): novo `tests/TesouroDireto.Web.Tests/` (bUnit) traz o **`TesouroDireto.Web` para dentro do gate** (`TesouroApiClient` + componentes Simulador/Tributos); com o Web na medição o floor caiu **94%→82%** (Web entra com 32%). **Limite aceito:** o gate agregado não trava a existência futura de testes de Web (removê-los *sobe* a média) — travar exigiria piso por-módulo. Só o `E2E.Tests` (Docker, job à parte) segue fora do `dotnet test`.
- **Drift de schema seed↔migrations**: `seed.sql` define schema manualmente em paralelo às migrations — mudança numa migration sem atualizar seed quebra E2E silenciosamente.
- **Capacidade/limites — medidos (tarefa 58, ✅ 2026-08-08):** suíte k6 **sob demanda** em `tests/load/` (fora da pipeline), baseline em [`docs/load/`](./load/README.md). **Baseline prod (ramp 200 VUs, 1 IP):** dois tetos — **borda nginx ~30 req/s/IP** (`429`+`Retry-After: 5`; fluxos leves `preco-atual`/`simulador` batem aqui) e **capacidade da VPS** (fluxos pesados `titulos`/`historico` saturam em latência p95 ~7s a ~21–23 req/s antes da borda; 0% de 5xx). `titulos` mantém ETag/304=100% sob carga. Alvo obrigatório sem default + guarda anti-prod (`ALLOW_PROD=1`). **Follow-up:** live-Grafana durante o run exige habilitar o remote-write receiver no Prometheus (hoje bloqueado por `--web.external-url=/prometheus/` no 3.13.2); cenário `site/circuits.js` (circuitos SignalR) só validado contra mock, falta 1º run contra Blazor real.

---

## 6. Autenticação, Identidade e Rate Limiting

> Consolida o subsistema de auth **fechado nas tarefas 59–67** (ADR [`docs/arch/ADR-auth.md`](./arch/ADR-auth.md), aprovado 2026-08-06). Substitui a chave única compartilhada por **API pública multi-cliente**: keys por cliente (hasheadas, self-service) + login Google no site (Web como BFF), com rate limiting em duas camadas de app além do nginx.

### Identidade (modelo de dados)

Dois agregados de identidade em `src/TesouroDireto.Domain` (detalhe físico das tabelas em §2):
- **`Usuario`** (`Usuarios/Usuario.cs`, `Entity<Guid>`): `GoogleSub` nullable até o 1º login (`Usuario.cs:29`), VO `Email` (normaliza `Trim().ToLowerInvariant()`, `Email.cs:11-33`), enum **`PapelUsuario` { User=0, Admin=1 }** (`PapelUsuario.cs:3-7`), e os campos de aprovação/auditoria `Aprovado`/`Ativo`/`AprovadoEm`/`AprovadoPor` (`Usuario.cs:33-37`). Nasce `Aprovado=false`, `Ativo=true` (`Usuario.cs:24-25`); transições por `Aprovar`/`Desativar`/`GarantirAdminAprovado`/`DefinirGoogleSub` (56-104). **`ativo` ≠ `aprovado`**: desativar é revogação de acesso; aprovar é o gate de 1º uso.
- **`ApiKey`** (`ApiKeys/ApiKey.cs`, `Entity<Guid>`): FK `DonoUsuarioId`, `Ativa` (revogável via `Revogar`, 59-63), `Prefixo` não-secreto para exibição, e o VO **`ApiKeyHash`** = SHA-256 hex lowercase (`ApiKeyHash.FromRawKey`, `ApiKeyHash.cs:13-20`). **Só o `hash` e o `prefixo` são persistidos — nunca a key em claro** (o texto puro é mostrado 1× na geração, tarefa 63).
- **Configs EF** (`Infrastructure/Persistence/Configurations/`): `UsuarioConfiguration.cs` (tabela `usuarios`, uniques `ix_usuarios_google_sub`/`ix_usuarios_email`, self-FK `aprovado_por` `Restrict`, `papel` via `HasConversion<string>`), `ApiKeyConfiguration.cs` (tabela `api_keys`, unique `ix_api_keys_hash`, FK `dono_usuario_id` `Restrict`).
- **Guard de boot** (`API/Extensions/ApiKeyGuard.cs`): fora de Development/Testing, aborta o boot se `ApiKey:Key` (a **service key**) for vazia ou um placeholder bloqueado (`CHANGE-ME-IN-PRODUCTION` ou `dev-local-key`, `ApiKeyGuard.cs:8-14`); chamado em `Program.cs:19`.

### Matriz de autenticação e autorização

O `ApiKeyMiddleware` (`API/Middleware/ApiKeyMiddleware.cs`, fluxo `InvokeAsync` 48-97) resolve a identidade **antes** das rotas e grava o tipo em `HttpContext.Items[IdentityKindItemsKey]` (`ApiKeyMiddleware.cs:17-19,104`). Os filtros de gestão (`API/Http/`) leem esses items.

| Credencial | Como valida | Identidade (`Items`) | O que libera |
|---|---|---|---|
| **Service key** (`X-Api-Key` == `ApiKey:Key`) | fast-path SHA-256 `FixedTimeEquals` **in-memory, sem banco** (`ApiKeyMiddleware.cs:73,194-205`) | `IdentityKind="service"` | **tudo** — negócio + gestão (`/me/*`, `/admin/*`); **isenta** do rate-limit por cliente (66) |
| **Client key** (`X-Api-Key` = `td_…`) | SHA-256 → `IApiKeyReadRepository.GetActiveByHashAsync` (`WHERE hash AND ativa`, cache 5min) (`ApiKeyMiddleware.cs:79,126-134`) | `IdentityKind="client"` + `RateLimit.ClienteId=apiKeyId` (`:104-109`) | **só rotas de negócio**; sujeita ao rate-limit por cliente (66); **reprovada** nos filtros de gestão |
| **Google OAuth** (Web/BFF) | cookie httpOnly 8h sliding (`Web/Program.cs:38-47`); 1º login `OnCreatingTicket`→`GoogleLoginService` (exige `email_verified`)→`POST /admin/usuarios/sync` (`Web/Program.cs:60-80`) | claim `Role`=papel; o Web assere o usuário à API via header **`X-Acting-User-Sub`** (google_sub) **sobre a service key** (`TesouroApiClient.cs:18,72`) | telas do site; a API materializa a autorização nos filtros de gestão |
| **Isentas** (`/health*`, `/metrics`, `/swagger`) | `IsExcludedPath` short-circuit — 1º check (`ApiKeyMiddleware.cs:50-54,168-172`; `ApiKey:ExcludedPaths`) | — | passam sem auth, **sem rate-limit e sem contar falha** |
| **Falha** (sem key / key inválida) | 401 `application/problem+json` | — (nada em `Items`; `unknown` é só label da métrica `RecordUnauthorized`) | conta no **limiter por IP** (67) → 429 ao estourar |

**Filtros de gestão** (`API/Http/`, `IEndpointFilter`; rejeição sempre **403 problem+json**) — todos exigem **service key** (`IsServiceIdentity`, `ApiKeyIdentityExtensions.cs:8-10`) porque `X-Acting-User-Sub` sozinho é forjável por qualquer client key (endurecimento da tarefa 63, fecha personificação):
- **`ServiceKeyOnlyFilter`** (só service key) → `POST /admin/usuarios/sync` (`UsuarioEndpoints.cs:25`; o sync é o 1º login, sem usuário aprovado ainda).
- **`AdminOnlyFilter`** (service key + `X-Acting-User-Sub` → `Usuario` com **Papel=Admin ∧ Aprovado ∧ Ativo**, `AdminOnlyFilter.cs:16-38`) → `GET /admin/usuarios?pendentes`, `POST /admin/usuarios/{sub}/aprovar|desativar` (`UsuarioEndpoints.cs:52,74,94`); guarda o admin em `Items` (fonte de `aprovado_por`).
- **`UsuarioAprovadoFilter`** (service key + acting-user **Aprovado ∧ Ativo**, sem exigir Admin, `UsuarioAprovadoFilter.cs:14-36`) → `GET/POST /me/keys`, `POST /me/keys/{id}/revogar` (`ApiKeyEndpoints.cs:24,45,67`).

Decorre da matriz acima: `/v1/me/*` e `/v1/admin/*` exigem **service key** e são, portanto, **BFF-only** — não fazem parte da superfície publicável a um integrador externo. A superfície de uma client key `td_…` é **só** as rotas de negócio (títulos, preços, tributos, simulador, importação). Relevante para quem for publicar a referência OpenAPI (→ **tarefa 73**).

### Rate limiting (3 camadas, chaves distintas)

Complementares, sem duplicar — cada uma particiona por uma chave diferente:
1. **nginx (borda) — por IP bruto:** flood-guard grosso, zone `api` 30 r/s (`infra/nginx/tesouro-direto.conf`), `limit_req_status 429`+`Retry-After`.
2. **Por IP, só falhas de auth (tarefa 67) — semântico:** `IAuthFailureRateLimiter`/`InMemoryAuthFailureRateLimiter` (`Infrastructure/RateLimiting/`, `PartitionedRateLimiter<string>` fixed-window por IP; **10/60s** de `RateLimiting:AuthFailure:*`, `appsettings.json:24-27`; registro `Infrastructure/DependencyInjection.cs:184-193`). **Consume-on-failure**: só key ausente/inválida chama `RegisterFailure(ip)` (`ApiKeyMiddleware.cs:63,86`) → 401 dentro do orçamento, **429 + `Retry-After`** ao estourar; **key válida nunca toca o limiter** (cliente legítimo jamais punido, mesmo de IP compartilhado). IP real via `app.UseForwardedHeaders()` (`Program.cs:23`; `XForwardedFor`, `KnownNetworks`/`KnownProxies` limpos, `API/DependencyInjection.cs:23-28`); `ForwardLimit=1` + `$proxy_add_x_forwarded_for` ⇒ IP não-forjável através do nginx.
3. **Por cliente (tarefa 66) — fino:** `AddRateLimiter` `GlobalLimiter` particionado por `Items[RateLimit.ClienteId]` (`API/DependencyInjection.cs:75-112`); **60/min** fixed-window (`RateLimiting:PermitLimit`/`WindowSeconds`); service key/isentas/sem-cliente → `NoLimiter`; **429 problem+json + `Retry-After`** via `OnRejected` (corpo herda `correlationId`/`traceId`). Store `IRateLimitStore` (`InMemoryRateLimitStore`; **gancho Redis declarado e não implementado** — `RedisRateLimitStore` lança `NotImplementedException`, troca por `RateLimiting:Store=Redis` sem reescrever o particionamento). Registro `Infrastructure/DependencyInjection.cs:166-182`.

**Ordem no pipeline** (`Program.cs`): `UseForwardedHeaders` (23) → … → `ApiKeyMiddleware` (38) → `UseRateLimiter` (39) → rotas. Falhas de auth fazem **short-circuit no middleware antes do `UseRateLimiter`** — por isso o limite por cliente (66) nunca as vê, e o limiter por IP (67) foi criado para cobri-las.

### Fragilidades
- **Rate limit em processo, não distribuído** — ambos os limiters (66 e 67) são in-memory por instância; em deploy multi-instância cada réplica conta a sua cota. O `RedisRateLimitStore` é só stub (`NotImplementedException`); o limiter por IP nem tem gancho Redis. Aceitável na topologia atual (instância única atrás do nginx).
- **Confiança na borda (BFF)** — `X-Forwarded-For` (IP do limiter 67) e `X-Acting-User-Sub` (usuário asserido) são confiáveis **só** porque a API é alcançável apenas via nginx na rede interna; exposição direta da API quebraria as duas premissas.
- **Janela de revogação da client key** — o lookup é cacheado 5min (`Caching:ApiKeys`); uma key revogada dentro da janela segue válida até o TTL expirar (esperado, ADR §3.5; `revogar` invalida o cache via `InvalidateApiKeys()`, mas o cache é por instância).
- **Eviction de partições por IP não medida sob carga** — o `PartitionedRateLimiter` do 67 cria uma partição por IP que falha; a limpeza de partições ociosas é do framework (mesma postura do 66), não medida sob flood de IPs distintos.

---

## 7. Recursos e Capacidade

### O que existe

5 serviços em `docker-compose.yml`: 4 sempre ativos — `app` (API), `web` (Blazor), `db` (Postgres), `alloy` — e 1 sob `profiles: ["load"]`, `prometheus` (`docker-compose.yml:210-217`, imagem `prom/prometheus:v3.10.0`, teto 72M), que existe **só** para o k6 fazer remote-write local durante teste de carga: mandar as métricas `k6_*` para a nuvem estouraria a cardinalidade do free tier justamente durante o teste; em repouso custa zero. A tarefa 77 (2026-08-15) removeu os 4 serviços de observabilidade local que existiam até a 77.3 (`grafana`, `loki`, `promtail`, `node-exporter`, 516 MB de teto somado) e os substituiu por um coletor único, `alloy` (`grafana/alloy:v1.18.1`, container `tesouro-direto-alloy`, UI em `127.0.0.1:12345`, config em `infra/alloy/config.alloy`), que faz scrape/tail local e `remote_write`/`loki.write` para o Grafana Cloud (ver §4). Desde a tarefa 80.1 (2026-08-16), o `alloy` **também se coleta**: `job="alloy"` em `infra/alloy/config.alloy`, 640 séries próprias na nuvem (14% do teto de 10k do free tier, ver §4) — fecha o buraco que impedia diagnosticar o próprio consumo de memória do coletor sem sessão SSH manual. O `alloy` **ainda não tem** `deploy.resources.limits` — ver Fragilidades abaixo. O nginx **não** é container: roda no **host** via systemd (`.github/workflows/deploy.yml`: `cp infra/nginx/tesouro-direto.conf /etc/nginx/sites-enabled/tesouro-direto` + `nginx -t && systemctl reload nginx`); o `infra/nginx/docker-compose.yml` (serviço `nginx: image: nginx:alpine`) é uma **variante não usada** pelo deploy real. Todas as portas publicadas em `127.0.0.1:*` (`docker-compose.yml`). Volumes nomeados: `pgdata`, `prometheus-data`, `alloy-data` (`docker-compose.yml:313-315`; os antigos `grafana-data`/`loki-data`/`promtail-positions` foram apagados da VPS na 77.4). Deploy (`.github/workflows/deploy.yml`, job `deploy`): `git fetch origin main && git reset --hard origin/main` (compose vem do git, não de `scp`), `docker compose build` **na própria VPS** (sem `--no-cache` desde a tarefa 74.0/PR #59 — o cache do BuildKit passou a ser reaproveitado entre deploys), `docker compose up -d --remove-orphans` (`deploy.yml:236`; o `--remove-orphans` entrou na 77.4 — sem ele, containers cujo serviço **saiu** do arquivo, como os 4 removidos nesta fase, ficariam de pé e o deploy fecharia VERDE sem liberar nada; um serviço apenas **desativado** por `profiles:`, como o `prometheus` sob `load`, não é órfão e o sweep não o alcança — refutado ao vivo no primeiro deploy real da 77.4), `docker compose --profile load rm -sf prometheus` (`deploy.yml:253`; limpa o Prometheus efêmero que um `run-load.sh --local` anterior possa ter deixado de pé, idempotente), e por fim `docker compose up -d --force-recreate --no-deps alloy` (`deploy.yml:257`; recriação forçada porque `infra/alloy/config.alloy` é bind-mount e `up -d` não recria um serviço só por conteúdo mudar — a mesma armadilha que afetava `prometheus`/`grafana`/`promtail` antes de serem removidos). A lista hard-coded `prometheus grafana promtail` que a 74.6 tinha nesse passo do workflow **não existe mais**.

### Capacidade medida

Resumo — detalhe completo em [`docs/load/README.md`](./load/README.md) §7.4. **Re-medido na tarefa 74.5 (2026-08-13) com os limites de recurso da 74.3 em vigor** — substitui a medição de 2026-08-09 abaixo, feita **sem nenhum limite de recurso** nos containers. VPS de produção: **1 vCPU / 2 GB**. **Teto ≈ 120 req/s** — a vazão atendida deixa de acompanhar a ofertada: 119,48 de 120 req/s ofertados, depois 138,82 de 150 já com erros; **capacidade limpa 100 req/s**; **joelho ≈ 100 req/s** — onde o p50 sai do platô de 195–200 ms. **Ressalva ao citar esse joelho:** o critério é o p50; a **cauda** degrada bem antes — o p95 triplica entre 25 e 50 req/s (222 → 749 ms, amostra grande, não é ruído). Quem dimensiona por p95 deve tratar ~40–50 req/s como o ponto de atenção, não 100. **Gargalo = disputa pelo único vCPU** (loadavg de 0,32 a 8,27 sob carga), **não** a cota de CPU do `app` (`cpus: "0.70"`), que levou só **0,1 s** de throttling nos 6m37s amostrados, contra 62,3 s do `promtail` no mesmo intervalo (delta da janela do run; `cpu.stat` é cumulativo desde o boot) — a estimativa de 15–25 req/s que o plano fazia (partindo de 0,15 vCPU **rígido**) ficou 5–8× abaixo do medido. `limit_req` da borda (`rate=30r/s burst=50`) **mantido por decisão do dono**: com o joelho em 100 req/s são precisos ~3,3 IPs abusivos simultâneos para alcançá-lo, e **nenhum 429 do dia é de usuário orgânico** — dos 753.454, 753.436 são do IP do próprio medidor e 18 são de um scanner, ou seja o limitador funcionando (ressalva: descontado o medidor o volume orgânico da janela é de poucas dezenas de requisições, então isso é evidência de não-atrapalho, não prova de alto volume). Site (Blazor): **~350 circuitos simultâneos** — com a ressalva de que eles **reciclavam a cada 75 s** (o `HOLD_MS` não chegou ao módulo que o lê), então a carga medida foi de reconexão contínua, não de hold sustentado —, com o `web` a 146,6 de 160 MiB (91%) — memória **não** é o gargalo do site; o teto real é a borda: `worker_connections 768` (1 worker nginx) ÷ 2 conexões por circuito proxiado = **384**, falhas a partir de 400 — os antigos "≥343 circuitos" (2026-08-09, abaixo) mediam esse mesmo teto de conexões da borda, não memória, como parecia à época. **Follow-up aberto:** elevar `worker_connections`/`Max open files` (soft 1024) é mudança de config de borda, não de recurso de container — subir `memory` do `web` não compra circuito.

**Medição anterior, histórica (2026-08-09, sem limite de recurso nenhum nos containers):** teto medido (§7.3, rate limit do nginx elevado temporariamente a 100 r/s/IP para achar o limite real) ~90–95 req/s com p95 ~1,3 s (CPU saturada, load ~2,9 num 1 vCPU), 0 erro; joelho em ~50–70 req/s (CPU a 100%, p95 começa a subir); acima de 95 req/s não foi forçado. Gargalo = CPU, não memória — confirmado em [`docs/load/profiling.md`](./load/profiling.md) §6: thread-pool sem starvation (`queue_length`~0), GC = 6,4% do wall, CPU ~100% de 1 core; o gargalo real antes das otimizações #48 (cache da version do ETag)/#49 (`NoResetOnClose` no Npgsql) era o Postgres (~129 mil execuções de 1 query = 79% do tempo de DB). Site: ≥343 circuitos SignalR sustentados com folga, a ~0,2–0,3 MiB de RAM por circuito.

### Fragilidades

- **~~Zero limite de recurso nos 8 containers~~ resolvido na tarefa 74.3 (PR #63)** — `deploy.resources.limits.memory` (teto rígido) + `cpu_shares` (peso, teto folgado em `cpus`) aplicados aos 8 containers que existiam **naquela fase** (antes da 77): `app` 256M/1024/0,70 · `web` 160M/512/0,40 · `db` 128M/1024/0,60 · `grafana` 160M/128/0,15 · `loki` 132M/128/0,15 · `prometheus` 48M/64/0,10 · `promtail` 36M/128/0,15 · `node-exporter` 16M/64/0,05 (memória/`cpu_shares`/teto de `cpus`), mais `pids_limit` dimensionado e `memswap_limit` (igual à memória em `app`/`db`). `docker-compose.profiling.yml` convertido no mesmo commit para não conflitar (`can't set distinct values on 'cpus' and 'deploy.resources.limits.cpus'`). **Estado pós-77 (2026-08-15):** `grafana`/`loki`/`promtail`/`node-exporter` saíram do compose (77.4); `prometheus` continua com teto 72M, mas só sob `profiles: ["load"]`; `app`/`web`/`db` mantêm os mesmos tetos. O novo serviço `alloy` ganhou teto de **192M** na 77.5 — ver fragilidade dedicada abaixo.
- **`alloy` com teto INTERINO de memória (192M), ainda não em vigor na VPS** — `docker-compose.yml` ganhou `deploy.resources.limits.memory: 192M` na 77.5 (branch a criar, não mergeada). É deliberadamente folgado (~3× o consumo medido: 62,34 MB de não-descartável em produção com o Alloy já fazendo scrape de host + os 4 fluxos de log, pico de 65,4 MB no gate de 24h em andamento), porque o consumo do Alloy sob ingestão **rejeitada** pelo Grafana Cloud nunca foi observado — quando os limites do free tier (10k séries, não os 310k de trial) entrarem em vigor em 17/08/2026, o `remote_write` bufferiza e retenta, e um teto apertado transformaria rejeição de ingestão em OOM do coletor, apagando a observabilidade no momento em que ela avisaria do problema. Reavaliar após 17/08. **Enquanto a branch não mergeia, o teto só existe no arquivo** — a VPS roda o commit `afb4fd0`, sem `deploy.resources.limits` no `alloy` (`docker inspect` mostra `memory.max=0`); passa a valer no próximo deploy.
- **~~Prometheus sem retenção e sem volume~~ já resolvido na tarefa 74.2 (esta entrada do mapa estava desatualizada mesmo antes da 77)** — hoje `docker-compose.yml` já traz `--storage.tsdb.retention.time=7d`+`--storage.tsdb.retention.size=256MB` e o volume nomeado `prometheus-data:/prometheus` (`docker-compose.yml:236-240`). **Estado pós-77:** o `prometheus` só sobe sob `profiles: ["load"]` — é efêmero por design, existe só para o k6 fazer remote-write local durante o teste de carga, não faz mais parte da observabilidade contínua (essa foi migrada para o Grafana Cloud via Alloy, ver §4).
- **~~5 imagens de infra em `:latest`~~ resolvido na tarefa 74.0/PR #59** — `docker-compose.yml` pinou as 5 então em uso: `grafana/grafana:12.4.1`, `grafana/loki:3.6.7`, `prom/prometheus:v3.10.0`, `prom/node-exporter:v1.12.1`, `grafana/promtail:3.6.8` (digest do índice verificado idêntico ao que já rodava na VPS — pin congelou a versão em uso, não foi upgrade). Deixou de ser fragilidade: novo `pull` não muda mais o footprint sozinho. **Estado pós-77:** das 5, só `prom/prometheus:v3.10.0` segue pinado no compose hoje (sob `profiles: ["load"]`) — as outras 4 saíram na 77.4; o novo `alloy` chegou pinado desde o início (`grafana/alloy:v1.18.1`, 77.0).
- **Build .NET roda na VPS durante o deploy** — `docker compose build` (sem `--no-cache` desde a tarefa 74.0/PR #59) com o SDK 8.0 (`Dockerfile`/`Dockerfile.web`: `FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build`). Os Dockerfiles foram enxutados na mesma tarefa: só os `.csproj` de `src/*/` são copiados e restaurados (o restore roda **um único projeto** — API ou Web —, não a solução inteira); `tests/` não entra mais em nenhum `COPY` nem `dotnet restore`. Ainda é pico de CPU/RAM concorrente com o stack em execução: **medido na tarefa 74.1/PR #60** (`docs/load/footprint.md` §3.4 e §5) em **840 MB** de RSS somado (p95 834), com os dois `dotnet publish` em paralelo num único vCPU, levando o host a 1444 MB usados e **293 MB de swap**. A comparação contra o pico de 610 MB de antes do PR #59 **não pôde ser fechada** — vem de outro método, e o medidor da 74.1 foi encontrado defeituoso nas duas direções antes de produzir este número (media o próprio `awk`; era cego ao `dotnet publish`). O que é comparável e mudou: RAM disponível no pior instante do deploy passou de **82 MB para 816 MB**. O pico de build **não é governado por limite nenhum do compose** (BuildKit roda no daemon, fora do cgroup do serviço) e seguirá fora do orçamento depois da 74.3 — a rede é o swapfile.
- **~~Nenhum alerta de CPU ou memória~~ resolvido na tarefa 74.6, migrado para a nuvem na 77.3** — as regras viviam em `infra/grafana/provisioning/alerting/rules.yaml`, **diretório hoje inexistente**; a fonte da verdade desde a 77.3 é `infra/grafana/cloud/rules.yaml`, aplicado ao Grafana Cloud por `scripts/grafana-cloud/apply-cloud.sh` (ver §4). Tem **21** regras (confirmado por leitura). As 8 de recurso por container cobrem: memória não-descartável >85%/>95% do teto, reclaim sustentado, throttling de CPU, reinício, OOM kill por métrica, obsolescência das próprias métricas (dead-man) e **OOM pelo log de kernel via Loki** — esta última independente de qualquer métrica de container, de propósito. Continua **não havendo cAdvisor** (rejeitado por custo: 60-120 MB de RSS contra ~42 MB de orçamento no bloco INFRA, decisão pré-77 preservada); as métricas por container vêm do **textfile collector**, hoje lido pelo `prometheus.exporter.unix` embutido no Alloy — substituiu o container `node-exporter` na 77.1/77.4, com os mesmos nomes de coletor e o mesmo textfile (`infra/alloy/config.alloy:24-65`) —, alimentado por `infra/host/container-metrics.sh` num systemd timer de 30s no host (instalação manual documentada em `infra/host/README.md`), lendo cgroup v2 direto. Os alertas de memória regem sobre `td_container_memory_unreclaimable_bytes` (`anon+shmem+kernel`), **não** sobre `working_set`: este último inclui page cache reclamável e acusaria risco de OOM onde só há cache (medido, historicamente, com o container `node-exporter` que já não existe mais: 78% de working_set contra 45% de não-descartável).
- **Rotação de log do Docker fora da pipeline** — `infra/host/daemon.json` (`max-size: 20m`, `max-file: 3`) é instalação **manual** no host (`infra/host/README.md`), não aplicada pelo deploy. **Conferido na VPS em 2026-08-09 (tarefa 74.0): está em vigor** — `/etc/docker/daemon.json` existe e é idêntico ao do repo. Continua sendo fragilidade de *processo* (nada garante que siga assim; uma VPS nova nasceria sem), não de estado atual.
- **Container fora deste compose divide a máquina** — `infra/nginx/nginx.conf:63-64` proxia `location /sonar/ { proxy_pass http://tesouro-direto-sonar:9000; }`; o SonarQube não está em nenhum compose deste repo. **Estado em 2026-08-09: o container `tesouro-direto-sonar` está parado** (`Exited (0)` há ~4 meses) e não consome nada; precisando de ≥1,5 GB (JVM+Elasticsearch), não caberia nesta máquina de qualquer forma — o proxy `/sonar/` aponta para um serviço morto. Havia também `mysql-weducate` (351 MB, de outra aplicação, com MySQL publicado em `0.0.0.0:8806`), **removido a pedido do dono na tarefa 74.0**, com dump preservado.
- **`docker-compose.override.yml` fantasma** — o deploy faz `git reset --hard origin/main` mas **não** roda `git clean` (comentário do próprio workflow: preservar o `.env` untracked); um `docker-compose.override.yml` deixado na VPS sobreviveria a todo `reset --hard` e seria mesclado **silenciosamente** pelo `docker compose` em cada `up -d`.
- **Duas pools de Npgsql, ambas no default de 100 conexões** — `Infrastructure/DependencyInjection.cs:42-51` monta **uma** `connectionString` (`NoResetOnClose=true`, tarefa #49) e a usa **duas vezes**: `AddDbContext<AppDbContext>(... UseNpgsql(connectionString))` (EF, escritas, linha 48-49) e `AddSingleton(NpgsqlDataSource.Create(connectionString))` (Dapper, leituras, linha 51) — duas pools **independentes**, nenhuma com `MaxPoolSize` explícito (default Npgsql = 100) ⇒ até **200** conexões possíveis contra um `postgres:16-alpine` de fábrica (`max_connections=100`; `docker-compose.yml` não tem `command:` de tuning para `db`). Inofensivo hoje porque a carga real nunca chega perto; sob teto de memória/conexões vira caminho de esgotamento. As duas nascem da mesma string, então um `MaxPoolSize` no `NpgsqlConnectionStringBuilder` já existente conserta as duas num ponto só. → **tarefa 74**.
- **`AddMemoryCache()` sem `SizeLimit`** — `Infrastructure/DependencyInjection.cs:55`, registro sem opções, alimentando os decorators `Cached*ReadRepository` com TTLs de `Caching:*` que chegam a 7 dias (Titulos 1d, Feriados 7d, PrecosTaxas 6h por `tituloId`, ApiKeys 5min, ContentVersion 10s). Sem teto de entradas nem de tamanho, o cache cresce até a memória do processo — hoje inofensivo porque o container não tem limite; sob um teto de memória vira o vetor de OOM mais provável do `app`. → **tarefa 74**.
- **~~Prometheus não faz self-scrape~~ resolvido na tarefa 74.2, premissa obsoleta desde a 77** — a 74.2 tinha levado `prometheus.yml` a **três** jobs (`tesouro-direto-api`, `node`, `prometheus`) para conseguir medir o próprio custo de memória do Prometheus local, que na época rodava continuamente. **Hoje isso não é mais verdade:** `prometheus.yml` (raiz do repo) tem **um único job**, `tesouro-direto-api` (`app:8080`, `prometheus.yml:17-21`, confirmado por leitura) — porque esse Prometheus só existe efêmero sob `profiles: ["load"]`, para o k6 fazer remote-write local durante teste de carga, e não roda mais continuamente (self-scrape do próprio processo deixou de ter valor). O scrape de `app:8080` **e** do host (`node`) que sustentava a observabilidade de produção passou para o **Grafana Alloy** (`prometheus.scrape "app"` / `prometheus.exporter.unix "host"` + `discovery.relabel` forçando `job="node"`, `infra/alloy/config.alloy:16-82`), que reenvia por `remote_write` ao Grafana Cloud.
- **Borda limita circuitos Blazor a ~384, não é memória de container** — medido na tarefa 74.5 (2026-08-13): `worker_connections 768` (1 worker) no nginx, e cada circuito Blazor proxiado consome 2 conexões ⇒ teto de conexões = 384, com falhas a partir de 400. Elevar exige mudar config de borda (`worker_connections`/`Max open files`, hoje soft 1024), não o `memory` do container `web` — refuta o entendimento anterior de que os "≥343 circuitos" medidos em 2026-08-09 fossem limitados por memória.
- **~~Bloco INFRA sem folga~~ endereçado na tarefa 76** — medido pela **não-descartável** (`td_container_memory_unreclaimable_bytes`, a métrica que o teto governa): `prometheus` **50,0 MiB contra teto de 48M — não cabia**, `grafana` 138,8/160 (87%), `loki` 110,1/132 (83%), `node-exporter` 9,0/16 (56%, confortável), tudo em **operação normal** (os testes de carga da 74.5 deram picos *menores*). Como o alerting é Grafana-managed, `grafana` morto por OOM calaria todos os alertas. A 76 elevou os tetos para ~70% de uso no pico (`prometheus` 72M, `grafana` 224M, `loki` 160M, `node-exporter` 24M), com o `GOMEMLIMIT` subindo junto — sem isso o runtime Go ignora a folga nova e a mudança vira no-op. **Custo assumido:** o total vai de 936 MB (47,6%) a 1060 MB (53,9%), rompendo de propósito a promessa de "50% livres" da tarefa 74. **Superado pela tarefa 77 (2026-08-15):** os containers medidos aqui como serviços contínuos (`grafana`, `loki`, `node-exporter`, e o `prometheus` de então) não existem mais nessa forma — os três primeiros saíram do compose (77.4) e o `prometheus` virou efêmero sob `profiles: ["load"]`; o coletor único que os substitui, `alloy`, mediu **51,99 MB** de não-descartável no portão de medição da 77.0 (limiar de 200 MB), bem abaixo da folga que esta fase (76) tinha comprado a esses 4 containers.

→ **tarefa 74**: ~~medir o custo real por container (`run-footprint.sh`, lendo cgroup v2 direto em 4 janelas — cold boot, ocioso, sob carga e durante o deploy), tunar, e só então fixar os tetos~~ **CONCLUÍDA (mandatória) em 2026-08-13** — meta do dono de **25% app / 25% infra compartilhada / 50% livres**: medição (74.1) → tuning (74.2) → tetos via `deploy.resources.limits`+`cpu_shares` (74.3, ~21% APP + ~26% INFRA = 47,6% da máquina) → healthchecks/gate de deploy (74.4a) → capacidade re-medida sob os tetos novos (74.5, ver "Capacidade medida" acima) → alertas por container + OOM via Loki (74.6). Na época, a única fase que restava era a **~~74.2b~~** (migração para Grafana Cloud), **opcional, dispensada pela própria medição** — o bloco INFRA local coube no orçamento depois do tuning. **Decisão revertida na tarefa 77 (2026-08-15, ver `docs/PLANO.md` Fase 77):** o Grafana Cloud foi adotado afinal — não pelo motivo original de footprint (a 76 já tinha resolvido isso, ao custo de subir o total da máquina a 47,6–53,9%), mas porque o alerting Grafana-managed **dentro** da VPS calava junto com ela se ela caísse; no Cloud, ausência de dado vira `NoData` e o Telegram toca — um dead-man's switch de graça que a stack local nunca teve. Somado ao `promtail` ter chegado ao fim de vida em março/2026. A decisão antiga (dispensar o Cloud) não estava errada para o problema que respondia (footprint); o problema mudou. Ver §4 e §7 acima para a topologia resultante.

---

## Verificação das fragilidades

Passo adversarial: o revisor tentou **refutar** cada fragilidade de maior impacto lendo o código-fonte real (não docs/comentários). **Nenhuma das 10 foi refutada** — todas confirmadas literalmente, com uma nuance agravante no item 8. **Status de correção** (coluna final, atualizado 2026-07-23): itens **1, 2, 3, 4, 5, 7, 8, 9 e 10 resolvidos** (tarefas 4, 2, 11, 9, 10, 3, 1, 7 e 8 do PLANO); segue **aberto** apenas o item **6** (upsert não-atômico).

| # | Afirmação | Veredito | Evidência |
|---|-----------|----------|-----------|
| 1 | Sem `JsonStringEnumConverter` → enums só aceitam número no POST | ✅ CONFIRMADO → ✔️ RESOLVIDO (tarefa 4) | Era: zero ocorrência de `JsonStringEnumConverter` em `src`. **Corrigido:** `JsonStringEnumConverter` registrado (aceita string E número); binding agora via `CreateTributoRequest` (tarefa 15). |
| 2 | `Indexador.FromName(v).Value` quebra materialização EF com valor fora da whitelist | ✅ CONFIRMADO → ✔️ RESOLVIDO (tarefa 2) | `Indexador.cs:11-24` (whitelist 4); `Result<T>.Value` lança em falha (`Common/Result.cs:47-48`); usado em `TituloConfiguration.cs:36`. **Corrigido:** `Indexador.FromPersistence` (lossless) na leitura EF. |
| 3 | Simulador chama BCB Focus ao vivo, sem cache/fallback | ✅ CONFIRMADO → ✔️ RESOLVIDO (tarefa 11) | Era: `SimularCommandHandler.cs:40-47` propagava falha sem fallback; `DependencyInjection.cs:82-85` registrava sem decorator `Cached*` (contraste com repos, linhas 40-73). **Corrigido:** `CachedProjecaoMercadoService` (`Infrastructure/Projecoes/`) registrado como `IProjecaoMercadoService` em `DependencyInjection.cs` (com `FocusBcbService` como colaborador interno); cache fresh 6h + fallback last-known-good 7 dias por indexador, fallback só em `Projecao.HttpError`. |
| 4 | Nenhum seed de tributos/feriados em produção | ✅ CONFIRMADO → ✔️ RESOLVIDO (tarefa 9) | Era: zero `.HasData(` em `src`, único `seed.sql` de teste. **Corrigido:** `InitializeDatabaseAsync` semeia tributos (via `SeedTributosCommand` idempotente) + importa feriados no 1º boot; verificado ao vivo em banco novo. |
| 5 | Import de feriados sem job Quartz (só endpoint manual) | ✅ CONFIRMADO → ✔️ RESOLVIDO (tarefa 10) | Era: `DependencyInjection.cs:87-96` só registrava `JobKey("csv-import")`; sem `FeriadoImportJob`; caminho único era `ImportacaoEndpoints.cs:19-25`. **Corrigido:** novo `FeriadoImportJob.cs` + `JobKey("feriado-import")`/`"feriado-import-trigger"` registrado em `DependencyInjection.cs` (cron `Feriados:CronSchedule`, fallback `0 0 6 1 12 ?`), disparando `ImportFeriadosCommand`. |
| 6 | `ix_titulos_tipo_vencimento` é UNIQUE e o upsert não é atômico | ✅ CONFIRMADO | `TituloConfiguration.cs:42-44` `.IsUnique()`; `GetOrCreateTituloAsync` (`ImportCsvCommandHandler.cs:107-132`) é check-then-act sem transação → race sob concorrência (UNIQUE evita duplicata mas lança exceção não tratada). |
| 7 | Healthcheck raso (`/health` string fixa, não toca banco) | ✅ CONFIRMADO → ✔️ RESOLVIDO (tarefa 3) | `Program.cs:29`; zero `AddHealthChecks`/`AddDbContextCheck`/`MapHealthChecks` em `src`. **Corrigido:** `AddDbContextCheck` + readiness/liveness. |
| 8 | Grafana público + senha default `admin` | ✅ CONFIRMADO (agravado) → ✔️ RESOLVIDO (tarefa 1) | `nginx/tesouro-direto.conf` `location /grafana/` sem `allow/deny` (vs `/prometheus/` que tem `allow 127.0.0.1; deny all`); `docker-compose.yml:67` `${GRAFANA_PASSWORD:-admin}`. **Exposição pública foi decisão recente e intencional** (commit `67406be`). **Corrigido:** `GRAFANA_PASSWORD:?` (sem fallback) + `/grafana/` restrito por IP (commit `ba3b103`). **Nota (tarefa 77, 2026-08-15):** o Grafana local e a rota `/grafana/` deixaram de existir — o Grafana hoje é o Grafana Cloud (SaaS), com controle de acesso próprio, fora do escopo desta mitigação de borda. |
| 9 | PUT tributos decide 404 vs 400 por `Error.Code.Contains("NotFound")` | ✅ CONFIRMADO → ✔️ RESOLVIDO (tarefa 7 → 48) | Era: substring match em `ConfiguracaoEndpoints.cs:33`. **Corrigido (7):** helper `ToHttpResult` mapeia por estrutura (`.EndsWith(".NotFound")`→404), aplicado nos 4 endpoints. **Tarefa 48 (2026-08-04):** substituído o `EndsWith` por **categoria explícita** (`enum ErrorType` no `Error`: Validation→400/NotFound→404/Conflict→409); `AlreadyExists`→409. |
| 10 | Zero teste de integração HTTP nas rotas de negócio | ✅ CONFIRMADO → ✔️ RESOLVIDO (tarefa 8) | Era: `WebApplicationFactory` só em 3 testes de middleware. **Corrigido:** suíte `Integration/` (Testcontainers) cobre Titulos/Simulador/Tributos/Importacao/Auth/Health; bug real de prod (Dapper/`DateOnly`) pego no caminho. |

**Conclusão do revisor:** 10/10 confirmadas contra o código real. As demais fragilidades listadas nas seções §1–§5 (sem verificação individual marcada) vêm da leitura dos agentes de mapeamento e são consistentes com o código, mas não passaram pelo passo adversarial explícito.

---

## Fragilidades priorizadas (síntese)

**Bloqueiam operação / corretude:**
1. **Sem seed versionado de tributos e feriados** ✔️ RESOLVIDO (tarefa 9): seed no boot (tributos idempotente + feriados no 1º boot) → Simulador funciona em banco novo (§4).
2. ✔️ RESOLVIDO (tarefa 10) **Feriados sem job agendado** → `FeriadoImportJob` (Quartz) faz o refresh contínuo (§4).
3. ✔️ RESOLVIDO (tarefa 11) **BCB Focus ao vivo, sem cache nem fallback** → `CachedProjecaoMercadoService` desacopla o simulador da disponibilidade do BCB (§3).
4. ✔️ RESOLVIDO (tarefa 2) **`Indexador` whitelist rígida** → materialização EF quebra com valor inesperado na coluna (§2).

**Segurança / operação:**
5. Chave de API única compartilhada, default `CHANGE-ME-IN-PRODUCTION`; endpoints mutantes com mesma proteção que leitura (§1). ✔️ RESOLVIDO (tarefas 5 + 19 + **onda de auth 59–67**, 2026-08-08): boot aborta em prod com chave default/vazia **ou `dev-local-key`** (5+19); e a **onda 59–67** (ADR [`docs/arch/ADR-auth.md`](./arch/ADR-auth.md), aprovado 2026-08-06) entregou a **API pública multi-cliente** — keys **por cliente** (hasheadas, self-service, validadas contra tabela), login Google no site (Web como BFF), seed de admin por `ADMIN_EMAIL`, gestão admin/self-service, e rate limiting por cliente (66) + por IP nas falhas (67). A service key vira credencial **BFF** (Web→API); clientes externos usam keys próprias. **Ver §6.** Resíduo aceito: identidade/rate-limit em processo (não distribuído).
6. ✔️ RESOLVIDO (tarefa 1) Grafana público com senha default `admin` (§4).
7. ✔️ RESOLVIDO (tarefa 13) Sem retry/circuit breaker em nenhuma integração externa → `AddResilienceHandler` nos 3 typed clients (retry+timeout nos 3, circuit breaker só no BCB) (§3).
8. ✔️ RESOLVIDO (tarefa 3) Healthcheck raso não detecta banco fora (§4).
9. ✔️ RESOLVIDO (tarefa 74, mandatória concluída 2026-08-13) **Sem orçamento de recursos** → tetos de CPU/memória nos containers (74.3) + volume nomeado da TSDB do Prometheus, que não perde mais dados a cada deploy (74.2) + capacidade re-medida sob os tetos novos (74.5) + alertas por container/OOM (74.6) (§7). Na época restava só a **~~74.2b~~** (Grafana Cloud), opcional, dispensada pela própria medição. **Atualização (tarefa 77, 2026-08-15):** a 74.2b foi executada afinal — não por footprint (já resolvido na 76), mas por resiliência de alerting: o alerting Grafana-managed local calava junto com a VPS se ela caísse; no Cloud, ausência de dado vira `NoData`→Telegram, um dead-man's switch de graça, somado ao fim de vida do `promtail`. A stack local (`grafana`/`loki`/`prometheus` contínuo/`promtail`/`node-exporter`) foi removida e substituída por um `alloy` único, que reenvia para o Grafana Cloud. Ver §4/§7.

**Qualidade / manutenção:**
10. Contrato HTTP vazando `CreateTributoCommand` ✔️ RESOLVIDO (tarefa 15); acesso à API duplicado em 5 páginas Blazor ✔️ RESOLVIDO (tarefa 16, typed client `TesouroApiClient`) (§1).
11. Enums só numéricos + `ParseEnum` hardcoded frágil ✔️ RESOLVIDO (tarefa 4) (§1).
12. Zero teste de integração de endpoint HTTP ✔️ RESOLVIDO (tarefa 8); gate de cobertura ✔️ RESOLVIDO (tarefa 18; Web trazido para o gate na tarefa 32, floor 94%→82%) (§5).
13. Observabilidade: métricas por caso de uso (O6) e por dependência externa (O7) ✔️ RESOLVIDAS (2026-07-23); **métricas de negócio** ✔️ RESOLVIDAS (tarefas 14/O8, 2026-07-24); Web no Loki/Grafana ✔️ RESOLVIDO (tarefa 17/O2); logs padronizados e painéis de nível corrigidos (tarefas O1/O3/20) (§4).
14. **API pública sem superfície de documentação** — client key entregue sem contrato; Swagger só por túnel SSH (§1) → **tarefa 73**.
